diff --git a/app/aston-villa/page.tsx b/app/aston-villa/page.tsx
index f995855..e8d80a1 100644
--- a/app/aston-villa/page.tsx
+++ b/app/aston-villa/page.tsx
@@ -2,34 +2,11 @@ import Link from 'next/link';
 import { supabase } from '../../lib/supabase';
 import ClubMapLoader from '../../components/ClubMapLoader';
 import SiteNav from '../../components/SiteNav';
+import FanGroupDirectory, { type FanGroupRow } from '../../components/FanGroupDirectory';
 
 
 export const revalidate = 3600; // refresh from Supabase at most once per hour
 
-type FanGroupRow = {
-  name: string;
-  city: string | null;
-  country: string | null;
-  latitude: number | null;
-  longitude: number | null;
-  url: string;
-  description: string | null;
-  region: string | null;
-  type: string | null;
-};
-
-function typeColor(type: string | null, clubColor: string) {
-  if (type === 'community') return '#FFFFFF';
-  if (type === 'fan_bar') return '#F97316';
-  return clubColor;
-}
-
-function typeLabel(type: string | null) {
-  if (type === 'community') return 'Fan community';
-  if (type === 'fan_bar') return 'Fan bar';
-  return 'Supporter club';
-}
-
 export default async function AstonVillaPage() {
   const { data: club } = await supabase
     .from('clubs')
@@ -66,20 +43,6 @@ export default async function AstonVillaPage() {
       description: g.description,
     }));
 
-  const regionMap = new Map<string, FanGroupRow[]>();
-  for (const g of groups) {
-    const region = g.region || 'More worldwide';
-    if (!regionMap.has(region)) regionMap.set(region, []);
-    regionMap.get(region)!.push(g);
-  }
-  const regionOrder = Array.from(regionMap.keys()).sort((a, b) => {
-    if (a === 'United Kingdom') return -1;
-    if (b === 'United Kingdom') return 1;
-    if (a === 'More worldwide') return 1;
-    if (b === 'More worldwide') return -1;
-    return a.localeCompare(b);
-  });
-
   return (
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
@@ -120,40 +83,7 @@ export default async function AstonVillaPage() {
         <p style={{ color: 'rgba(255,255,255,0.4)', fontSize: '14px', marginBottom: '48px' }}>
           Official supporter clubs and fan communities worldwide. Click any group to visit their site.
         </p>
-        {regionOrder.map(region => (
-          <div key={region} style={{ marginBottom: '48px' }}>
-            <h3 style={{ fontSize: '13px', fontWeight: 600, letterSpacing: '0.1em', textTransform: 'uppercase', color: CLUB_COLOR, marginBottom: '16px', paddingBottom: '8px', borderBottom: `1px solid rgba(108,171,221,0.2)` }}>
-              {region}
-            </h3>
-            <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
-              {regionMap.get(region)!.map(group => (
-                <a key={group.name} href={group.url} target="_blank" rel="noopener noreferrer" className="tg-fan-card"
-                  style={{ display: 'block', textDecoration: 'none', background: 'rgba(255,255,255,0.03)', border: '1px solid rgba(255,255,255,0.07)', borderRadius: '10px', padding: '16px 20px' }}
-                >
-                  <div style={{ display: 'flex', alignItems: 'flex-start', justifyContent: 'space-between', gap: '12px', flexWrap: 'wrap' }}>
-                    <div style={{ flex: 1, minWidth: '200px' }}>
-                      <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '6px' }}>
-                        <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: typeColor(group.type, CLUB_COLOR), flexShrink: 0 }} />
-                        <span style={{ fontSize: '15px', fontWeight: 600, color: '#fff' }}>{group.name}</span>
-                        <span style={{ fontSize: '10px', fontWeight: 500, letterSpacing: '0.06em', textTransform: 'uppercase', color: typeColor(group.type, CLUB_COLOR), opacity: 0.8 }}>{typeLabel(group.type)}</span>
-                      </div>
-                      <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.35)', marginBottom: '6px' }}>{group.city ? `${group.city} · ${group.country}` : group.country}</div>
-                      {group.description && (
-                        <div style={{ fontSize: '13px', color: 'rgba(255,255,255,0.55)', lineHeight: 1.5 }}>{group.description}</div>
-                      )}
-                    </div>
-                    <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.25)', flexShrink: 0, paddingTop: '2px' }}>Visit →</div>
-                  </div>
-                </a>
-              ))}
-            </div>
-          </div>
-        ))}
-
-        <style>{`
-          .tg-fan-card { transition: border-color 0.15s, background 0.15s; }
-          .tg-fan-card:hover { border-color: ${CLUB_COLOR}55; background: rgba(255,255,255,0.05); }
-        `}</style>
+        <FanGroupDirectory groups={groups} clubColor={CLUB_COLOR} />
 
         <div style={{ marginTop: '48px', padding: '28px', borderRadius: '12px', border: '1px dashed rgba(255,255,255,0.12)', textAlign: 'center' }}>
           <p style={{ fontSize: '15px', fontWeight: 600, marginBottom: '8px' }}>Is your group missing?</p>
diff --git a/app/bournemouth/page.tsx b/app/bournemouth/page.tsx
index 630146b..9702009 100644
--- a/app/bournemouth/page.tsx
+++ b/app/bournemouth/page.tsx
@@ -2,34 +2,11 @@ import Link from 'next/link';
 import { supabase } from '../../lib/supabase';
 import ClubMapLoader from '../../components/ClubMapLoader';
 import SiteNav from '../../components/SiteNav';
+import FanGroupDirectory, { type FanGroupRow } from '../../components/FanGroupDirectory';
 
 
 export const revalidate = 3600; // refresh from Supabase at most once per hour
 
-type FanGroupRow = {
-  name: string;
-  city: string | null;
-  country: string | null;
-  latitude: number | null;
-  longitude: number | null;
-  url: string;
-  description: string | null;
-  region: string | null;
-  type: string | null;
-};
-
-function typeColor(type: string | null, clubColor: string) {
-  if (type === 'community') return '#FFFFFF';
-  if (type === 'fan_bar') return '#F97316';
-  return clubColor;
-}
-
-function typeLabel(type: string | null) {
-  if (type === 'community') return 'Fan community';
-  if (type === 'fan_bar') return 'Fan bar';
-  return 'Supporter club';
-}
-
 export default async function BournemouthPage() {
   const { data: club } = await supabase
     .from('clubs')
@@ -66,20 +43,6 @@ export default async function BournemouthPage() {
       description: g.description,
     }));
 
-  const regionMap = new Map<string, FanGroupRow[]>();
-  for (const g of groups) {
-    const region = g.region || 'More worldwide';
-    if (!regionMap.has(region)) regionMap.set(region, []);
-    regionMap.get(region)!.push(g);
-  }
-  const regionOrder = Array.from(regionMap.keys()).sort((a, b) => {
-    if (a === 'United Kingdom') return -1;
-    if (b === 'United Kingdom') return 1;
-    if (a === 'More worldwide') return 1;
-    if (b === 'More worldwide') return -1;
-    return a.localeCompare(b);
-  });
-
   return (
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
@@ -120,40 +83,7 @@ export default async function BournemouthPage() {
         <p style={{ color: 'rgba(255,255,255,0.4)', fontSize: '14px', marginBottom: '48px' }}>
           Official supporter clubs and fan communities worldwide. Click any group to visit their site.
         </p>
-        {regionOrder.map(region => (
-          <div key={region} style={{ marginBottom: '48px' }}>
-            <h3 style={{ fontSize: '13px', fontWeight: 600, letterSpacing: '0.1em', textTransform: 'uppercase', color: CLUB_COLOR, marginBottom: '16px', paddingBottom: '8px', borderBottom: `1px solid rgba(108,171,221,0.2)` }}>
-              {region}
-            </h3>
-            <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
-              {regionMap.get(region)!.map(group => (
-                <a key={group.name} href={group.url} target="_blank" rel="noopener noreferrer" className="tg-fan-card"
-                  style={{ display: 'block', textDecoration: 'none', background: 'rgba(255,255,255,0.03)', border: '1px solid rgba(255,255,255,0.07)', borderRadius: '10px', padding: '16px 20px' }}
-                >
-                  <div style={{ display: 'flex', alignItems: 'flex-start', justifyContent: 'space-between', gap: '12px', flexWrap: 'wrap' }}>
-                    <div style={{ flex: 1, minWidth: '200px' }}>
-                      <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '6px' }}>
-                        <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: typeColor(group.type, CLUB_COLOR), flexShrink: 0 }} />
-                        <span style={{ fontSize: '15px', fontWeight: 600, color: '#fff' }}>{group.name}</span>
-                        <span style={{ fontSize: '10px', fontWeight: 500, letterSpacing: '0.06em', textTransform: 'uppercase', color: typeColor(group.type, CLUB_COLOR), opacity: 0.8 }}>{typeLabel(group.type)}</span>
-                      </div>
-                      <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.35)', marginBottom: '6px' }}>{group.city ? `${group.city} · ${group.country}` : group.country}</div>
-                      {group.description && (
-                        <div style={{ fontSize: '13px', color: 'rgba(255,255,255,0.55)', lineHeight: 1.5 }}>{group.description}</div>
-                      )}
-                    </div>
-                    <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.25)', flexShrink: 0, paddingTop: '2px' }}>Visit →</div>
-                  </div>
-                </a>
-              ))}
-            </div>
-          </div>
-        ))}
-
-        <style>{`
-          .tg-fan-card { transition: border-color 0.15s, background 0.15s; }
-          .tg-fan-card:hover { border-color: ${CLUB_COLOR}55; background: rgba(255,255,255,0.05); }
-        `}</style>
+        <FanGroupDirectory groups={groups} clubColor={CLUB_COLOR} />
 
         <div style={{ marginTop: '48px', padding: '28px', borderRadius: '12px', border: '1px dashed rgba(255,255,255,0.12)', textAlign: 'center' }}>
           <p style={{ fontSize: '15px', fontWeight: 600, marginBottom: '8px' }}>Is your group missing?</p>
diff --git a/app/brentford/page.tsx b/app/brentford/page.tsx
index ba65c12..2978e62 100644
--- a/app/brentford/page.tsx
+++ b/app/brentford/page.tsx
@@ -2,34 +2,11 @@ import Link from 'next/link';
 import { supabase } from '../../lib/supabase';
 import ClubMapLoader from '../../components/ClubMapLoader';
 import SiteNav from '../../components/SiteNav';
+import FanGroupDirectory, { type FanGroupRow } from '../../components/FanGroupDirectory';
 
 
 export const revalidate = 3600; // refresh from Supabase at most once per hour
 
-type FanGroupRow = {
-  name: string;
-  city: string | null;
-  country: string | null;
-  latitude: number | null;
-  longitude: number | null;
-  url: string;
-  description: string | null;
-  region: string | null;
-  type: string | null;
-};
-
-function typeColor(type: string | null, clubColor: string) {
-  if (type === 'community') return '#FFFFFF';
-  if (type === 'fan_bar') return '#F97316';
-  return clubColor;
-}
-
-function typeLabel(type: string | null) {
-  if (type === 'community') return 'Fan community';
-  if (type === 'fan_bar') return 'Fan bar';
-  return 'Supporter club';
-}
-
 export default async function BrentfordPage() {
   const { data: club } = await supabase
     .from('clubs')
@@ -66,20 +43,6 @@ export default async function BrentfordPage() {
       description: g.description,
     }));
 
-  const regionMap = new Map<string, FanGroupRow[]>();
-  for (const g of groups) {
-    const region = g.region || 'More worldwide';
-    if (!regionMap.has(region)) regionMap.set(region, []);
-    regionMap.get(region)!.push(g);
-  }
-  const regionOrder = Array.from(regionMap.keys()).sort((a, b) => {
-    if (a === 'United Kingdom') return -1;
-    if (b === 'United Kingdom') return 1;
-    if (a === 'More worldwide') return 1;
-    if (b === 'More worldwide') return -1;
-    return a.localeCompare(b);
-  });
-
   return (
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
@@ -120,40 +83,7 @@ export default async function BrentfordPage() {
         <p style={{ color: 'rgba(255,255,255,0.4)', fontSize: '14px', marginBottom: '48px' }}>
           Official supporter clubs and fan communities worldwide. Click any group to visit their site.
         </p>
-        {regionOrder.map(region => (
-          <div key={region} style={{ marginBottom: '48px' }}>
-            <h3 style={{ fontSize: '13px', fontWeight: 600, letterSpacing: '0.1em', textTransform: 'uppercase', color: CLUB_COLOR, marginBottom: '16px', paddingBottom: '8px', borderBottom: `1px solid rgba(108,171,221,0.2)` }}>
-              {region}
-            </h3>
-            <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
-              {regionMap.get(region)!.map(group => (
-                <a key={group.name} href={group.url} target="_blank" rel="noopener noreferrer" className="tg-fan-card"
-                  style={{ display: 'block', textDecoration: 'none', background: 'rgba(255,255,255,0.03)', border: '1px solid rgba(255,255,255,0.07)', borderRadius: '10px', padding: '16px 20px' }}
-                >
-                  <div style={{ display: 'flex', alignItems: 'flex-start', justifyContent: 'space-between', gap: '12px', flexWrap: 'wrap' }}>
-                    <div style={{ flex: 1, minWidth: '200px' }}>
-                      <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '6px' }}>
-                        <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: typeColor(group.type, CLUB_COLOR), flexShrink: 0 }} />
-                        <span style={{ fontSize: '15px', fontWeight: 600, color: '#fff' }}>{group.name}</span>
-                        <span style={{ fontSize: '10px', fontWeight: 500, letterSpacing: '0.06em', textTransform: 'uppercase', color: typeColor(group.type, CLUB_COLOR), opacity: 0.8 }}>{typeLabel(group.type)}</span>
-                      </div>
-                      <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.35)', marginBottom: '6px' }}>{group.city ? `${group.city} · ${group.country}` : group.country}</div>
-                      {group.description && (
-                        <div style={{ fontSize: '13px', color: 'rgba(255,255,255,0.55)', lineHeight: 1.5 }}>{group.description}</div>
-                      )}
-                    </div>
-                    <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.25)', flexShrink: 0, paddingTop: '2px' }}>Visit →</div>
-                  </div>
-                </a>
-              ))}
-            </div>
-          </div>
-        ))}
-
-        <style>{`
-          .tg-fan-card { transition: border-color 0.15s, background 0.15s; }
-          .tg-fan-card:hover { border-color: ${CLUB_COLOR}55; background: rgba(255,255,255,0.05); }
-        `}</style>
+        <FanGroupDirectory groups={groups} clubColor={CLUB_COLOR} />
 
         <div style={{ marginTop: '48px', padding: '28px', borderRadius: '12px', border: '1px dashed rgba(255,255,255,0.12)', textAlign: 'center' }}>
           <p style={{ fontSize: '15px', fontWeight: 600, marginBottom: '8px' }}>Is your group missing?</p>
diff --git a/app/brighton/page.tsx b/app/brighton/page.tsx
index cc33ac2..299c490 100644
--- a/app/brighton/page.tsx
+++ b/app/brighton/page.tsx
@@ -2,34 +2,11 @@ import Link from 'next/link';
 import { supabase } from '../../lib/supabase';
 import ClubMapLoader from '../../components/ClubMapLoader';
 import SiteNav from '../../components/SiteNav';
+import FanGroupDirectory, { type FanGroupRow } from '../../components/FanGroupDirectory';
 
 
 export const revalidate = 3600; // refresh from Supabase at most once per hour
 
-type FanGroupRow = {
-  name: string;
-  city: string | null;
-  country: string | null;
-  latitude: number | null;
-  longitude: number | null;
-  url: string;
-  description: string | null;
-  region: string | null;
-  type: string | null;
-};
-
-function typeColor(type: string | null, clubColor: string) {
-  if (type === 'community') return '#FFFFFF';
-  if (type === 'fan_bar') return '#F97316';
-  return clubColor;
-}
-
-function typeLabel(type: string | null) {
-  if (type === 'community') return 'Fan community';
-  if (type === 'fan_bar') return 'Fan bar';
-  return 'Supporter club';
-}
-
 export default async function BrightonPage() {
   const { data: club } = await supabase
     .from('clubs')
@@ -66,20 +43,6 @@ export default async function BrightonPage() {
       description: g.description,
     }));
 
-  const regionMap = new Map<string, FanGroupRow[]>();
-  for (const g of groups) {
-    const region = g.region || 'More worldwide';
-    if (!regionMap.has(region)) regionMap.set(region, []);
-    regionMap.get(region)!.push(g);
-  }
-  const regionOrder = Array.from(regionMap.keys()).sort((a, b) => {
-    if (a === 'United Kingdom') return -1;
-    if (b === 'United Kingdom') return 1;
-    if (a === 'More worldwide') return 1;
-    if (b === 'More worldwide') return -1;
-    return a.localeCompare(b);
-  });
-
   return (
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
@@ -120,40 +83,7 @@ export default async function BrightonPage() {
         <p style={{ color: 'rgba(255,255,255,0.4)', fontSize: '14px', marginBottom: '48px' }}>
           Official supporter clubs and fan communities worldwide. Click any group to visit their site.
         </p>
-        {regionOrder.map(region => (
-          <div key={region} style={{ marginBottom: '48px' }}>
-            <h3 style={{ fontSize: '13px', fontWeight: 600, letterSpacing: '0.1em', textTransform: 'uppercase', color: CLUB_COLOR, marginBottom: '16px', paddingBottom: '8px', borderBottom: `1px solid rgba(108,171,221,0.2)` }}>
-              {region}
-            </h3>
-            <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
-              {regionMap.get(region)!.map(group => (
-                <a key={group.name} href={group.url} target="_blank" rel="noopener noreferrer" className="tg-fan-card"
-                  style={{ display: 'block', textDecoration: 'none', background: 'rgba(255,255,255,0.03)', border: '1px solid rgba(255,255,255,0.07)', borderRadius: '10px', padding: '16px 20px' }}
-                >
-                  <div style={{ display: 'flex', alignItems: 'flex-start', justifyContent: 'space-between', gap: '12px', flexWrap: 'wrap' }}>
-                    <div style={{ flex: 1, minWidth: '200px' }}>
-                      <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '6px' }}>
-                        <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: typeColor(group.type, CLUB_COLOR), flexShrink: 0 }} />
-                        <span style={{ fontSize: '15px', fontWeight: 600, color: '#fff' }}>{group.name}</span>
-                        <span style={{ fontSize: '10px', fontWeight: 500, letterSpacing: '0.06em', textTransform: 'uppercase', color: typeColor(group.type, CLUB_COLOR), opacity: 0.8 }}>{typeLabel(group.type)}</span>
-                      </div>
-                      <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.35)', marginBottom: '6px' }}>{group.city ? `${group.city} · ${group.country}` : group.country}</div>
-                      {group.description && (
-                        <div style={{ fontSize: '13px', color: 'rgba(255,255,255,0.55)', lineHeight: 1.5 }}>{group.description}</div>
-                      )}
-                    </div>
-                    <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.25)', flexShrink: 0, paddingTop: '2px' }}>Visit →</div>
-                  </div>
-                </a>
-              ))}
-            </div>
-          </div>
-        ))}
-
-        <style>{`
-          .tg-fan-card { transition: border-color 0.15s, background 0.15s; }
-          .tg-fan-card:hover { border-color: ${CLUB_COLOR}55; background: rgba(255,255,255,0.05); }
-        `}</style>
+        <FanGroupDirectory groups={groups} clubColor={CLUB_COLOR} />
 
         <div style={{ marginTop: '48px', padding: '28px', borderRadius: '12px', border: '1px dashed rgba(255,255,255,0.12)', textAlign: 'center' }}>
           <p style={{ fontSize: '15px', fontWeight: 600, marginBottom: '8px' }}>Is your group missing?</p>
diff --git a/app/celtic/page.tsx b/app/celtic/page.tsx
index f025677..24bc286 100644
--- a/app/celtic/page.tsx
+++ b/app/celtic/page.tsx
@@ -2,34 +2,11 @@ import Link from 'next/link';
 import { supabase } from '../../lib/supabase';
 import ClubMapLoader from '../../components/ClubMapLoader';
 import SiteNav from '../../components/SiteNav';
+import FanGroupDirectory, { type FanGroupRow } from '../../components/FanGroupDirectory';
 
 
 export const revalidate = 3600; // refresh from Supabase at most once per hour
 
-type FanGroupRow = {
-  name: string;
-  city: string | null;
-  country: string | null;
-  latitude: number | null;
-  longitude: number | null;
-  url: string;
-  description: string | null;
-  region: string | null;
-  type: string | null;
-};
-
-function typeColor(type: string | null, clubColor: string) {
-  if (type === 'community') return '#FFFFFF';
-  if (type === 'fan_bar') return '#F97316';
-  return clubColor;
-}
-
-function typeLabel(type: string | null) {
-  if (type === 'community') return 'Fan community';
-  if (type === 'fan_bar') return 'Fan bar';
-  return 'Supporter club';
-}
-
 export default async function CelticPage() {
   const { data: club } = await supabase
     .from('clubs')
@@ -66,20 +43,6 @@ export default async function CelticPage() {
       description: g.description,
     }));
 
-  const regionMap = new Map<string, FanGroupRow[]>();
-  for (const g of groups) {
-    const region = g.region || 'More worldwide';
-    if (!regionMap.has(region)) regionMap.set(region, []);
-    regionMap.get(region)!.push(g);
-  }
-  const regionOrder = Array.from(regionMap.keys()).sort((a, b) => {
-    if (a === 'United Kingdom') return -1;
-    if (b === 'United Kingdom') return 1;
-    if (a === 'More worldwide') return 1;
-    if (b === 'More worldwide') return -1;
-    return a.localeCompare(b);
-  });
-
   return (
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
@@ -120,40 +83,7 @@ export default async function CelticPage() {
         <p style={{ color: 'rgba(255,255,255,0.4)', fontSize: '14px', marginBottom: '48px' }}>
           Official supporter clubs and fan communities worldwide. Click any group to visit their site.
         </p>
-        {regionOrder.map(region => (
-          <div key={region} style={{ marginBottom: '48px' }}>
-            <h3 style={{ fontSize: '13px', fontWeight: 600, letterSpacing: '0.1em', textTransform: 'uppercase', color: CLUB_COLOR, marginBottom: '16px', paddingBottom: '8px', borderBottom: `1px solid rgba(108,171,221,0.2)` }}>
-              {region}
-            </h3>
-            <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
-              {regionMap.get(region)!.map(group => (
-                <a key={group.name} href={group.url} target="_blank" rel="noopener noreferrer" className="tg-fan-card"
-                  style={{ display: 'block', textDecoration: 'none', background: 'rgba(255,255,255,0.03)', border: '1px solid rgba(255,255,255,0.07)', borderRadius: '10px', padding: '16px 20px' }}
-                >
-                  <div style={{ display: 'flex', alignItems: 'flex-start', justifyContent: 'space-between', gap: '12px', flexWrap: 'wrap' }}>
-                    <div style={{ flex: 1, minWidth: '200px' }}>
-                      <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '6px' }}>
-                        <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: typeColor(group.type, CLUB_COLOR), flexShrink: 0 }} />
-                        <span style={{ fontSize: '15px', fontWeight: 600, color: '#fff' }}>{group.name}</span>
-                        <span style={{ fontSize: '10px', fontWeight: 500, letterSpacing: '0.06em', textTransform: 'uppercase', color: typeColor(group.type, CLUB_COLOR), opacity: 0.8 }}>{typeLabel(group.type)}</span>
-                      </div>
-                      <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.35)', marginBottom: '6px' }}>{group.city ? `${group.city} · ${group.country}` : group.country}</div>
-                      {group.description && (
-                        <div style={{ fontSize: '13px', color: 'rgba(255,255,255,0.55)', lineHeight: 1.5 }}>{group.description}</div>
-                      )}
-                    </div>
-                    <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.25)', flexShrink: 0, paddingTop: '2px' }}>Visit →</div>
-                  </div>
-                </a>
-              ))}
-            </div>
-          </div>
-        ))}
-
-        <style>{`
-          .tg-fan-card { transition: border-color 0.15s, background 0.15s; }
-          .tg-fan-card:hover { border-color: ${CLUB_COLOR}55; background: rgba(255,255,255,0.05); }
-        `}</style>
+        <FanGroupDirectory groups={groups} clubColor={CLUB_COLOR} />
 
         <div style={{ marginTop: '48px', padding: '28px', borderRadius: '12px', border: '1px dashed rgba(255,255,255,0.12)', textAlign: 'center' }}>
           <p style={{ fontSize: '15px', fontWeight: 600, marginBottom: '8px' }}>Is your group missing?</p>
diff --git a/app/chelsea/page.tsx b/app/chelsea/page.tsx
index 38f511c..6697836 100644
--- a/app/chelsea/page.tsx
+++ b/app/chelsea/page.tsx
@@ -2,34 +2,11 @@ import Link from 'next/link';
 import { supabase } from '../../lib/supabase';
 import ClubMapLoader from '../../components/ClubMapLoader';
 import SiteNav from '../../components/SiteNav';
+import FanGroupDirectory, { type FanGroupRow } from '../../components/FanGroupDirectory';
 
 
 export const revalidate = 3600; // refresh from Supabase at most once per hour
 
-type FanGroupRow = {
-  name: string;
-  city: string | null;
-  country: string | null;
-  latitude: number | null;
-  longitude: number | null;
-  url: string;
-  description: string | null;
-  region: string | null;
-  type: string | null;
-};
-
-function typeColor(type: string | null, clubColor: string) {
-  if (type === 'community') return '#FFFFFF';
-  if (type === 'fan_bar') return '#F97316';
-  return clubColor;
-}
-
-function typeLabel(type: string | null) {
-  if (type === 'community') return 'Fan community';
-  if (type === 'fan_bar') return 'Fan bar';
-  return 'Supporter club';
-}
-
 export default async function ChelseaPage() {
   const { data: club } = await supabase
     .from('clubs')
@@ -66,20 +43,6 @@ export default async function ChelseaPage() {
       description: g.description,
     }));
 
-  const regionMap = new Map<string, FanGroupRow[]>();
-  for (const g of groups) {
-    const region = g.region || 'More worldwide';
-    if (!regionMap.has(region)) regionMap.set(region, []);
-    regionMap.get(region)!.push(g);
-  }
-  const regionOrder = Array.from(regionMap.keys()).sort((a, b) => {
-    if (a === 'United Kingdom') return -1;
-    if (b === 'United Kingdom') return 1;
-    if (a === 'More worldwide') return 1;
-    if (b === 'More worldwide') return -1;
-    return a.localeCompare(b);
-  });
-
   return (
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
@@ -120,40 +83,7 @@ export default async function ChelseaPage() {
         <p style={{ color: 'rgba(255,255,255,0.4)', fontSize: '14px', marginBottom: '48px' }}>
           Official supporter clubs and fan communities worldwide. Click any group to visit their site.
         </p>
-        {regionOrder.map(region => (
-          <div key={region} style={{ marginBottom: '48px' }}>
-            <h3 style={{ fontSize: '13px', fontWeight: 600, letterSpacing: '0.1em', textTransform: 'uppercase', color: CLUB_COLOR, marginBottom: '16px', paddingBottom: '8px', borderBottom: `1px solid rgba(108,171,221,0.2)` }}>
-              {region}
-            </h3>
-            <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
-              {regionMap.get(region)!.map(group => (
-                <a key={group.name} href={group.url} target="_blank" rel="noopener noreferrer" className="tg-fan-card"
-                  style={{ display: 'block', textDecoration: 'none', background: 'rgba(255,255,255,0.03)', border: '1px solid rgba(255,255,255,0.07)', borderRadius: '10px', padding: '16px 20px' }}
-                >
-                  <div style={{ display: 'flex', alignItems: 'flex-start', justifyContent: 'space-between', gap: '12px', flexWrap: 'wrap' }}>
-                    <div style={{ flex: 1, minWidth: '200px' }}>
-                      <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '6px' }}>
-                        <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: typeColor(group.type, CLUB_COLOR), flexShrink: 0 }} />
-                        <span style={{ fontSize: '15px', fontWeight: 600, color: '#fff' }}>{group.name}</span>
-                        <span style={{ fontSize: '10px', fontWeight: 500, letterSpacing: '0.06em', textTransform: 'uppercase', color: typeColor(group.type, CLUB_COLOR), opacity: 0.8 }}>{typeLabel(group.type)}</span>
-                      </div>
-                      <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.35)', marginBottom: '6px' }}>{group.city ? `${group.city} · ${group.country}` : group.country}</div>
-                      {group.description && (
-                        <div style={{ fontSize: '13px', color: 'rgba(255,255,255,0.55)', lineHeight: 1.5 }}>{group.description}</div>
-                      )}
-                    </div>
-                    <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.25)', flexShrink: 0, paddingTop: '2px' }}>Visit →</div>
-                  </div>
-                </a>
-              ))}
-            </div>
-          </div>
-        ))}
-
-        <style>{`
-          .tg-fan-card { transition: border-color 0.15s, background 0.15s; }
-          .tg-fan-card:hover { border-color: ${CLUB_COLOR}55; background: rgba(255,255,255,0.05); }
-        `}</style>
+        <FanGroupDirectory groups={groups} clubColor={CLUB_COLOR} />
 
         <div style={{ marginTop: '48px', padding: '28px', borderRadius: '12px', border: '1px dashed rgba(255,255,255,0.12)', textAlign: 'center' }}>
           <p style={{ fontSize: '15px', fontWeight: 600, marginBottom: '8px' }}>Is your group missing?</p>
diff --git a/app/coventry/page.tsx b/app/coventry/page.tsx
index 8feaba6..231d55c 100644
--- a/app/coventry/page.tsx
+++ b/app/coventry/page.tsx
@@ -2,34 +2,11 @@ import Link from 'next/link';
 import { supabase } from '../../lib/supabase';
 import ClubMapLoader from '../../components/ClubMapLoader';
 import SiteNav from '../../components/SiteNav';
+import FanGroupDirectory, { type FanGroupRow } from '../../components/FanGroupDirectory';
 
 
 export const revalidate = 3600; // refresh from Supabase at most once per hour
 
-type FanGroupRow = {
-  name: string;
-  city: string | null;
-  country: string | null;
-  latitude: number | null;
-  longitude: number | null;
-  url: string;
-  description: string | null;
-  region: string | null;
-  type: string | null;
-};
-
-function typeColor(type: string | null, clubColor: string) {
-  if (type === 'community') return '#FFFFFF';
-  if (type === 'fan_bar') return '#F97316';
-  return clubColor;
-}
-
-function typeLabel(type: string | null) {
-  if (type === 'community') return 'Fan community';
-  if (type === 'fan_bar') return 'Fan bar';
-  return 'Supporter club';
-}
-
 export default async function CoventryPage() {
   const { data: club } = await supabase
     .from('clubs')
@@ -66,20 +43,6 @@ export default async function CoventryPage() {
       description: g.description,
     }));
 
-  const regionMap = new Map<string, FanGroupRow[]>();
-  for (const g of groups) {
-    const region = g.region || 'More worldwide';
-    if (!regionMap.has(region)) regionMap.set(region, []);
-    regionMap.get(region)!.push(g);
-  }
-  const regionOrder = Array.from(regionMap.keys()).sort((a, b) => {
-    if (a === 'United Kingdom') return -1;
-    if (b === 'United Kingdom') return 1;
-    if (a === 'More worldwide') return 1;
-    if (b === 'More worldwide') return -1;
-    return a.localeCompare(b);
-  });
-
   return (
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
@@ -120,40 +83,7 @@ export default async function CoventryPage() {
         <p style={{ color: 'rgba(255,255,255,0.4)', fontSize: '14px', marginBottom: '48px' }}>
           Official supporter clubs and fan communities worldwide. Click any group to visit their site.
         </p>
-        {regionOrder.map(region => (
-          <div key={region} style={{ marginBottom: '48px' }}>
-            <h3 style={{ fontSize: '13px', fontWeight: 600, letterSpacing: '0.1em', textTransform: 'uppercase', color: CLUB_COLOR, marginBottom: '16px', paddingBottom: '8px', borderBottom: `1px solid rgba(108,171,221,0.2)` }}>
-              {region}
-            </h3>
-            <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
-              {regionMap.get(region)!.map(group => (
-                <a key={group.name} href={group.url} target="_blank" rel="noopener noreferrer" className="tg-fan-card"
-                  style={{ display: 'block', textDecoration: 'none', background: 'rgba(255,255,255,0.03)', border: '1px solid rgba(255,255,255,0.07)', borderRadius: '10px', padding: '16px 20px' }}
-                >
-                  <div style={{ display: 'flex', alignItems: 'flex-start', justifyContent: 'space-between', gap: '12px', flexWrap: 'wrap' }}>
-                    <div style={{ flex: 1, minWidth: '200px' }}>
-                      <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '6px' }}>
-                        <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: typeColor(group.type, CLUB_COLOR), flexShrink: 0 }} />
-                        <span style={{ fontSize: '15px', fontWeight: 600, color: '#fff' }}>{group.name}</span>
-                        <span style={{ fontSize: '10px', fontWeight: 500, letterSpacing: '0.06em', textTransform: 'uppercase', color: typeColor(group.type, CLUB_COLOR), opacity: 0.8 }}>{typeLabel(group.type)}</span>
-                      </div>
-                      <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.35)', marginBottom: '6px' }}>{group.city ? `${group.city} · ${group.country}` : group.country}</div>
-                      {group.description && (
-                        <div style={{ fontSize: '13px', color: 'rgba(255,255,255,0.55)', lineHeight: 1.5 }}>{group.description}</div>
-                      )}
-                    </div>
-                    <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.25)', flexShrink: 0, paddingTop: '2px' }}>Visit →</div>
-                  </div>
-                </a>
-              ))}
-            </div>
-          </div>
-        ))}
-
-        <style>{`
-          .tg-fan-card { transition: border-color 0.15s, background 0.15s; }
-          .tg-fan-card:hover { border-color: ${CLUB_COLOR}55; background: rgba(255,255,255,0.05); }
-        `}</style>
+        <FanGroupDirectory groups={groups} clubColor={CLUB_COLOR} />
 
         <div style={{ marginTop: '48px', padding: '28px', borderRadius: '12px', border: '1px dashed rgba(255,255,255,0.12)', textAlign: 'center' }}>
           <p style={{ fontSize: '15px', fontWeight: 600, marginBottom: '8px' }}>Is your group missing?</p>
diff --git a/app/crystal-palace/page.tsx b/app/crystal-palace/page.tsx
index 5797d26..17f5e74 100644
--- a/app/crystal-palace/page.tsx
+++ b/app/crystal-palace/page.tsx
@@ -2,34 +2,11 @@ import Link from 'next/link';
 import { supabase } from '../../lib/supabase';
 import ClubMapLoader from '../../components/ClubMapLoader';
 import SiteNav from '../../components/SiteNav';
+import FanGroupDirectory, { type FanGroupRow } from '../../components/FanGroupDirectory';
 
 
 export const revalidate = 3600; // refresh from Supabase at most once per hour
 
-type FanGroupRow = {
-  name: string;
-  city: string | null;
-  country: string | null;
-  latitude: number | null;
-  longitude: number | null;
-  url: string;
-  description: string | null;
-  region: string | null;
-  type: string | null;
-};
-
-function typeColor(type: string | null, clubColor: string) {
-  if (type === 'community') return '#FFFFFF';
-  if (type === 'fan_bar') return '#F97316';
-  return clubColor;
-}
-
-function typeLabel(type: string | null) {
-  if (type === 'community') return 'Fan community';
-  if (type === 'fan_bar') return 'Fan bar';
-  return 'Supporter club';
-}
-
 export default async function CrystalPalacePage() {
   const { data: club } = await supabase
     .from('clubs')
@@ -66,20 +43,6 @@ export default async function CrystalPalacePage() {
       description: g.description,
     }));
 
-  const regionMap = new Map<string, FanGroupRow[]>();
-  for (const g of groups) {
-    const region = g.region || 'More worldwide';
-    if (!regionMap.has(region)) regionMap.set(region, []);
-    regionMap.get(region)!.push(g);
-  }
-  const regionOrder = Array.from(regionMap.keys()).sort((a, b) => {
-    if (a === 'United Kingdom') return -1;
-    if (b === 'United Kingdom') return 1;
-    if (a === 'More worldwide') return 1;
-    if (b === 'More worldwide') return -1;
-    return a.localeCompare(b);
-  });
-
   return (
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
@@ -120,40 +83,7 @@ export default async function CrystalPalacePage() {
         <p style={{ color: 'rgba(255,255,255,0.4)', fontSize: '14px', marginBottom: '48px' }}>
           Official supporter clubs and fan communities worldwide. Click any group to visit their site.
         </p>
-        {regionOrder.map(region => (
-          <div key={region} style={{ marginBottom: '48px' }}>
-            <h3 style={{ fontSize: '13px', fontWeight: 600, letterSpacing: '0.1em', textTransform: 'uppercase', color: CLUB_COLOR, marginBottom: '16px', paddingBottom: '8px', borderBottom: `1px solid rgba(108,171,221,0.2)` }}>
-              {region}
-            </h3>
-            <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
-              {regionMap.get(region)!.map(group => (
-                <a key={group.name} href={group.url} target="_blank" rel="noopener noreferrer" className="tg-fan-card"
-                  style={{ display: 'block', textDecoration: 'none', background: 'rgba(255,255,255,0.03)', border: '1px solid rgba(255,255,255,0.07)', borderRadius: '10px', padding: '16px 20px' }}
-                >
-                  <div style={{ display: 'flex', alignItems: 'flex-start', justifyContent: 'space-between', gap: '12px', flexWrap: 'wrap' }}>
-                    <div style={{ flex: 1, minWidth: '200px' }}>
-                      <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '6px' }}>
-                        <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: typeColor(group.type, CLUB_COLOR), flexShrink: 0 }} />
-                        <span style={{ fontSize: '15px', fontWeight: 600, color: '#fff' }}>{group.name}</span>
-                        <span style={{ fontSize: '10px', fontWeight: 500, letterSpacing: '0.06em', textTransform: 'uppercase', color: typeColor(group.type, CLUB_COLOR), opacity: 0.8 }}>{typeLabel(group.type)}</span>
-                      </div>
-                      <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.35)', marginBottom: '6px' }}>{group.city ? `${group.city} · ${group.country}` : group.country}</div>
-                      {group.description && (
-                        <div style={{ fontSize: '13px', color: 'rgba(255,255,255,0.55)', lineHeight: 1.5 }}>{group.description}</div>
-                      )}
-                    </div>
-                    <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.25)', flexShrink: 0, paddingTop: '2px' }}>Visit →</div>
-                  </div>
-                </a>
-              ))}
-            </div>
-          </div>
-        ))}
-
-        <style>{`
-          .tg-fan-card { transition: border-color 0.15s, background 0.15s; }
-          .tg-fan-card:hover { border-color: ${CLUB_COLOR}55; background: rgba(255,255,255,0.05); }
-        `}</style>
+        <FanGroupDirectory groups={groups} clubColor={CLUB_COLOR} />
 
         <div style={{ marginTop: '48px', padding: '28px', borderRadius: '12px', border: '1px dashed rgba(255,255,255,0.12)', textAlign: 'center' }}>
           <p style={{ fontSize: '15px', fontWeight: 600, marginBottom: '8px' }}>Is your group missing?</p>
diff --git a/app/everton/page.tsx b/app/everton/page.tsx
index 560afb0..978853e 100644
--- a/app/everton/page.tsx
+++ b/app/everton/page.tsx
@@ -2,34 +2,11 @@ import Link from 'next/link';
 import { supabase } from '../../lib/supabase';
 import ClubMapLoader from '../../components/ClubMapLoader';
 import SiteNav from '../../components/SiteNav';
+import FanGroupDirectory, { type FanGroupRow } from '../../components/FanGroupDirectory';
 
 
 export const revalidate = 3600; // refresh from Supabase at most once per hour
 
-type FanGroupRow = {
-  name: string;
-  city: string | null;
-  country: string | null;
-  latitude: number | null;
-  longitude: number | null;
-  url: string;
-  description: string | null;
-  region: string | null;
-  type: string | null;
-};
-
-function typeColor(type: string | null, clubColor: string) {
-  if (type === 'community') return '#FFFFFF';
-  if (type === 'fan_bar') return '#F97316';
-  return clubColor;
-}
-
-function typeLabel(type: string | null) {
-  if (type === 'community') return 'Fan community';
-  if (type === 'fan_bar') return 'Fan bar';
-  return 'Supporter club';
-}
-
 export default async function EvertonPage() {
   const { data: club } = await supabase
     .from('clubs')
@@ -66,20 +43,6 @@ export default async function EvertonPage() {
       description: g.description,
     }));
 
-  const regionMap = new Map<string, FanGroupRow[]>();
-  for (const g of groups) {
-    const region = g.region || 'More worldwide';
-    if (!regionMap.has(region)) regionMap.set(region, []);
-    regionMap.get(region)!.push(g);
-  }
-  const regionOrder = Array.from(regionMap.keys()).sort((a, b) => {
-    if (a === 'United Kingdom') return -1;
-    if (b === 'United Kingdom') return 1;
-    if (a === 'More worldwide') return 1;
-    if (b === 'More worldwide') return -1;
-    return a.localeCompare(b);
-  });
-
   return (
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
@@ -120,40 +83,7 @@ export default async function EvertonPage() {
         <p style={{ color: 'rgba(255,255,255,0.4)', fontSize: '14px', marginBottom: '48px' }}>
           Official supporter clubs and fan communities worldwide. Click any group to visit their site.
         </p>
-        {regionOrder.map(region => (
-          <div key={region} style={{ marginBottom: '48px' }}>
-            <h3 style={{ fontSize: '13px', fontWeight: 600, letterSpacing: '0.1em', textTransform: 'uppercase', color: CLUB_COLOR, marginBottom: '16px', paddingBottom: '8px', borderBottom: `1px solid rgba(108,171,221,0.2)` }}>
-              {region}
-            </h3>
-            <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
-              {regionMap.get(region)!.map(group => (
-                <a key={group.name} href={group.url} target="_blank" rel="noopener noreferrer" className="tg-fan-card"
-                  style={{ display: 'block', textDecoration: 'none', background: 'rgba(255,255,255,0.03)', border: '1px solid rgba(255,255,255,0.07)', borderRadius: '10px', padding: '16px 20px' }}
-                >
-                  <div style={{ display: 'flex', alignItems: 'flex-start', justifyContent: 'space-between', gap: '12px', flexWrap: 'wrap' }}>
-                    <div style={{ flex: 1, minWidth: '200px' }}>
-                      <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '6px' }}>
-                        <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: typeColor(group.type, CLUB_COLOR), flexShrink: 0 }} />
-                        <span style={{ fontSize: '15px', fontWeight: 600, color: '#fff' }}>{group.name}</span>
-                        <span style={{ fontSize: '10px', fontWeight: 500, letterSpacing: '0.06em', textTransform: 'uppercase', color: typeColor(group.type, CLUB_COLOR), opacity: 0.8 }}>{typeLabel(group.type)}</span>
-                      </div>
-                      <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.35)', marginBottom: '6px' }}>{group.city ? `${group.city} · ${group.country}` : group.country}</div>
-                      {group.description && (
-                        <div style={{ fontSize: '13px', color: 'rgba(255,255,255,0.55)', lineHeight: 1.5 }}>{group.description}</div>
-                      )}
-                    </div>
-                    <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.25)', flexShrink: 0, paddingTop: '2px' }}>Visit →</div>
-                  </div>
-                </a>
-              ))}
-            </div>
-          </div>
-        ))}
-
-        <style>{`
-          .tg-fan-card { transition: border-color 0.15s, background 0.15s; }
-          .tg-fan-card:hover { border-color: ${CLUB_COLOR}55; background: rgba(255,255,255,0.05); }
-        `}</style>
+        <FanGroupDirectory groups={groups} clubColor={CLUB_COLOR} />
 
         <div style={{ marginTop: '48px', padding: '28px', borderRadius: '12px', border: '1px dashed rgba(255,255,255,0.12)', textAlign: 'center' }}>
           <p style={{ fontSize: '15px', fontWeight: 600, marginBottom: '8px' }}>Is your group missing?</p>
diff --git a/app/fulham/page.tsx b/app/fulham/page.tsx
index d7994ca..3cf6e82 100644
--- a/app/fulham/page.tsx
+++ b/app/fulham/page.tsx
@@ -2,34 +2,11 @@ import Link from 'next/link';
 import { supabase } from '../../lib/supabase';
 import ClubMapLoader from '../../components/ClubMapLoader';
 import SiteNav from '../../components/SiteNav';
+import FanGroupDirectory, { type FanGroupRow } from '../../components/FanGroupDirectory';
 
 
 export const revalidate = 3600; // refresh from Supabase at most once per hour
 
-type FanGroupRow = {
-  name: string;
-  city: string | null;
-  country: string | null;
-  latitude: number | null;
-  longitude: number | null;
-  url: string;
-  description: string | null;
-  region: string | null;
-  type: string | null;
-};
-
-function typeColor(type: string | null, clubColor: string) {
-  if (type === 'community') return '#FFFFFF';
-  if (type === 'fan_bar') return '#F97316';
-  return clubColor;
-}
-
-function typeLabel(type: string | null) {
-  if (type === 'community') return 'Fan community';
-  if (type === 'fan_bar') return 'Fan bar';
-  return 'Supporter club';
-}
-
 export default async function FulhamPage() {
   const { data: club } = await supabase
     .from('clubs')
@@ -66,20 +43,6 @@ export default async function FulhamPage() {
       description: g.description,
     }));
 
-  const regionMap = new Map<string, FanGroupRow[]>();
-  for (const g of groups) {
-    const region = g.region || 'More worldwide';
-    if (!regionMap.has(region)) regionMap.set(region, []);
-    regionMap.get(region)!.push(g);
-  }
-  const regionOrder = Array.from(regionMap.keys()).sort((a, b) => {
-    if (a === 'United Kingdom') return -1;
-    if (b === 'United Kingdom') return 1;
-    if (a === 'More worldwide') return 1;
-    if (b === 'More worldwide') return -1;
-    return a.localeCompare(b);
-  });
-
   return (
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
@@ -120,40 +83,7 @@ export default async function FulhamPage() {
         <p style={{ color: 'rgba(255,255,255,0.4)', fontSize: '14px', marginBottom: '48px' }}>
           Official supporter clubs and fan communities worldwide. Click any group to visit their site.
         </p>
-        {regionOrder.map(region => (
-          <div key={region} style={{ marginBottom: '48px' }}>
-            <h3 style={{ fontSize: '13px', fontWeight: 600, letterSpacing: '0.1em', textTransform: 'uppercase', color: CLUB_COLOR, marginBottom: '16px', paddingBottom: '8px', borderBottom: `1px solid rgba(108,171,221,0.2)` }}>
-              {region}
-            </h3>
-            <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
-              {regionMap.get(region)!.map(group => (
-                <a key={group.name} href={group.url} target="_blank" rel="noopener noreferrer" className="tg-fan-card"
-                  style={{ display: 'block', textDecoration: 'none', background: 'rgba(255,255,255,0.03)', border: '1px solid rgba(255,255,255,0.07)', borderRadius: '10px', padding: '16px 20px' }}
-                >
-                  <div style={{ display: 'flex', alignItems: 'flex-start', justifyContent: 'space-between', gap: '12px', flexWrap: 'wrap' }}>
-                    <div style={{ flex: 1, minWidth: '200px' }}>
-                      <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '6px' }}>
-                        <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: typeColor(group.type, CLUB_COLOR), flexShrink: 0 }} />
-                        <span style={{ fontSize: '15px', fontWeight: 600, color: '#fff' }}>{group.name}</span>
-                        <span style={{ fontSize: '10px', fontWeight: 500, letterSpacing: '0.06em', textTransform: 'uppercase', color: typeColor(group.type, CLUB_COLOR), opacity: 0.8 }}>{typeLabel(group.type)}</span>
-                      </div>
-                      <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.35)', marginBottom: '6px' }}>{group.city ? `${group.city} · ${group.country}` : group.country}</div>
-                      {group.description && (
-                        <div style={{ fontSize: '13px', color: 'rgba(255,255,255,0.55)', lineHeight: 1.5 }}>{group.description}</div>
-                      )}
-                    </div>
-                    <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.25)', flexShrink: 0, paddingTop: '2px' }}>Visit →</div>
-                  </div>
-                </a>
-              ))}
-            </div>
-          </div>
-        ))}
-
-        <style>{`
-          .tg-fan-card { transition: border-color 0.15s, background 0.15s; }
-          .tg-fan-card:hover { border-color: ${CLUB_COLOR}55; background: rgba(255,255,255,0.05); }
-        `}</style>
+        <FanGroupDirectory groups={groups} clubColor={CLUB_COLOR} />
 
         <div style={{ marginTop: '48px', padding: '28px', borderRadius: '12px', border: '1px dashed rgba(255,255,255,0.12)', textAlign: 'center' }}>
           <p style={{ fontSize: '15px', fontWeight: 600, marginBottom: '8px' }}>Is your group missing?</p>
diff --git a/app/hull/page.tsx b/app/hull/page.tsx
index 4589c8d..a890def 100644
--- a/app/hull/page.tsx
+++ b/app/hull/page.tsx
@@ -2,34 +2,11 @@ import Link from 'next/link';
 import { supabase } from '../../lib/supabase';
 import ClubMapLoader from '../../components/ClubMapLoader';
 import SiteNav from '../../components/SiteNav';
+import FanGroupDirectory, { type FanGroupRow } from '../../components/FanGroupDirectory';
 
 
 export const revalidate = 3600; // refresh from Supabase at most once per hour
 
-type FanGroupRow = {
-  name: string;
-  city: string | null;
-  country: string | null;
-  latitude: number | null;
-  longitude: number | null;
-  url: string;
-  description: string | null;
-  region: string | null;
-  type: string | null;
-};
-
-function typeColor(type: string | null, clubColor: string) {
-  if (type === 'community') return '#FFFFFF';
-  if (type === 'fan_bar') return '#F97316';
-  return clubColor;
-}
-
-function typeLabel(type: string | null) {
-  if (type === 'community') return 'Fan community';
-  if (type === 'fan_bar') return 'Fan bar';
-  return 'Supporter club';
-}
-
 export default async function HullPage() {
   const { data: club } = await supabase
     .from('clubs')
@@ -66,20 +43,6 @@ export default async function HullPage() {
       description: g.description,
     }));
 
-  const regionMap = new Map<string, FanGroupRow[]>();
-  for (const g of groups) {
-    const region = g.region || 'More worldwide';
-    if (!regionMap.has(region)) regionMap.set(region, []);
-    regionMap.get(region)!.push(g);
-  }
-  const regionOrder = Array.from(regionMap.keys()).sort((a, b) => {
-    if (a === 'United Kingdom') return -1;
-    if (b === 'United Kingdom') return 1;
-    if (a === 'More worldwide') return 1;
-    if (b === 'More worldwide') return -1;
-    return a.localeCompare(b);
-  });
-
   return (
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
@@ -120,40 +83,7 @@ export default async function HullPage() {
         <p style={{ color: 'rgba(255,255,255,0.4)', fontSize: '14px', marginBottom: '48px' }}>
           Official supporter clubs and fan communities worldwide. Click any group to visit their site.
         </p>
-        {regionOrder.map(region => (
-          <div key={region} style={{ marginBottom: '48px' }}>
-            <h3 style={{ fontSize: '13px', fontWeight: 600, letterSpacing: '0.1em', textTransform: 'uppercase', color: CLUB_COLOR, marginBottom: '16px', paddingBottom: '8px', borderBottom: `1px solid rgba(108,171,221,0.2)` }}>
-              {region}
-            </h3>
-            <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
-              {regionMap.get(region)!.map(group => (
-                <a key={group.name} href={group.url} target="_blank" rel="noopener noreferrer" className="tg-fan-card"
-                  style={{ display: 'block', textDecoration: 'none', background: 'rgba(255,255,255,0.03)', border: '1px solid rgba(255,255,255,0.07)', borderRadius: '10px', padding: '16px 20px' }}
-                >
-                  <div style={{ display: 'flex', alignItems: 'flex-start', justifyContent: 'space-between', gap: '12px', flexWrap: 'wrap' }}>
-                    <div style={{ flex: 1, minWidth: '200px' }}>
-                      <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '6px' }}>
-                        <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: typeColor(group.type, CLUB_COLOR), flexShrink: 0 }} />
-                        <span style={{ fontSize: '15px', fontWeight: 600, color: '#fff' }}>{group.name}</span>
-                        <span style={{ fontSize: '10px', fontWeight: 500, letterSpacing: '0.06em', textTransform: 'uppercase', color: typeColor(group.type, CLUB_COLOR), opacity: 0.8 }}>{typeLabel(group.type)}</span>
-                      </div>
-                      <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.35)', marginBottom: '6px' }}>{group.city ? `${group.city} · ${group.country}` : group.country}</div>
-                      {group.description && (
-                        <div style={{ fontSize: '13px', color: 'rgba(255,255,255,0.55)', lineHeight: 1.5 }}>{group.description}</div>
-                      )}
-                    </div>
-                    <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.25)', flexShrink: 0, paddingTop: '2px' }}>Visit →</div>
-                  </div>
-                </a>
-              ))}
-            </div>
-          </div>
-        ))}
-
-        <style>{`
-          .tg-fan-card { transition: border-color 0.15s, background 0.15s; }
-          .tg-fan-card:hover { border-color: ${CLUB_COLOR}55; background: rgba(255,255,255,0.05); }
-        `}</style>
+        <FanGroupDirectory groups={groups} clubColor={CLUB_COLOR} />
 
         <div style={{ marginTop: '48px', padding: '28px', borderRadius: '12px', border: '1px dashed rgba(255,255,255,0.12)', textAlign: 'center' }}>
           <p style={{ fontSize: '15px', fontWeight: 600, marginBottom: '8px' }}>Is your group missing?</p>
diff --git a/app/ipswich/page.tsx b/app/ipswich/page.tsx
index fb8c8a7..f207265 100644
--- a/app/ipswich/page.tsx
+++ b/app/ipswich/page.tsx
@@ -2,34 +2,11 @@ import Link from 'next/link';
 import { supabase } from '../../lib/supabase';
 import ClubMapLoader from '../../components/ClubMapLoader';
 import SiteNav from '../../components/SiteNav';
+import FanGroupDirectory, { type FanGroupRow } from '../../components/FanGroupDirectory';
 
 
 export const revalidate = 3600; // refresh from Supabase at most once per hour
 
-type FanGroupRow = {
-  name: string;
-  city: string | null;
-  country: string | null;
-  latitude: number | null;
-  longitude: number | null;
-  url: string;
-  description: string | null;
-  region: string | null;
-  type: string | null;
-};
-
-function typeColor(type: string | null, clubColor: string) {
-  if (type === 'community') return '#FFFFFF';
-  if (type === 'fan_bar') return '#F97316';
-  return clubColor;
-}
-
-function typeLabel(type: string | null) {
-  if (type === 'community') return 'Fan community';
-  if (type === 'fan_bar') return 'Fan bar';
-  return 'Supporter club';
-}
-
 export default async function IpswichPage() {
   const { data: club } = await supabase
     .from('clubs')
@@ -66,20 +43,6 @@ export default async function IpswichPage() {
       description: g.description,
     }));
 
-  const regionMap = new Map<string, FanGroupRow[]>();
-  for (const g of groups) {
-    const region = g.region || 'More worldwide';
-    if (!regionMap.has(region)) regionMap.set(region, []);
-    regionMap.get(region)!.push(g);
-  }
-  const regionOrder = Array.from(regionMap.keys()).sort((a, b) => {
-    if (a === 'United Kingdom') return -1;
-    if (b === 'United Kingdom') return 1;
-    if (a === 'More worldwide') return 1;
-    if (b === 'More worldwide') return -1;
-    return a.localeCompare(b);
-  });
-
   return (
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
@@ -120,40 +83,7 @@ export default async function IpswichPage() {
         <p style={{ color: 'rgba(255,255,255,0.4)', fontSize: '14px', marginBottom: '48px' }}>
           Official supporter clubs and fan communities worldwide. Click any group to visit their site.
         </p>
-        {regionOrder.map(region => (
-          <div key={region} style={{ marginBottom: '48px' }}>
-            <h3 style={{ fontSize: '13px', fontWeight: 600, letterSpacing: '0.1em', textTransform: 'uppercase', color: CLUB_COLOR, marginBottom: '16px', paddingBottom: '8px', borderBottom: `1px solid rgba(108,171,221,0.2)` }}>
-              {region}
-            </h3>
-            <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
-              {regionMap.get(region)!.map(group => (
-                <a key={group.name} href={group.url} target="_blank" rel="noopener noreferrer" className="tg-fan-card"
-                  style={{ display: 'block', textDecoration: 'none', background: 'rgba(255,255,255,0.03)', border: '1px solid rgba(255,255,255,0.07)', borderRadius: '10px', padding: '16px 20px' }}
-                >
-                  <div style={{ display: 'flex', alignItems: 'flex-start', justifyContent: 'space-between', gap: '12px', flexWrap: 'wrap' }}>
-                    <div style={{ flex: 1, minWidth: '200px' }}>
-                      <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '6px' }}>
-                        <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: typeColor(group.type, CLUB_COLOR), flexShrink: 0 }} />
-                        <span style={{ fontSize: '15px', fontWeight: 600, color: '#fff' }}>{group.name}</span>
-                        <span style={{ fontSize: '10px', fontWeight: 500, letterSpacing: '0.06em', textTransform: 'uppercase', color: typeColor(group.type, CLUB_COLOR), opacity: 0.8 }}>{typeLabel(group.type)}</span>
-                      </div>
-                      <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.35)', marginBottom: '6px' }}>{group.city ? `${group.city} · ${group.country}` : group.country}</div>
-                      {group.description && (
-                        <div style={{ fontSize: '13px', color: 'rgba(255,255,255,0.55)', lineHeight: 1.5 }}>{group.description}</div>
-                      )}
-                    </div>
-                    <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.25)', flexShrink: 0, paddingTop: '2px' }}>Visit →</div>
-                  </div>
-                </a>
-              ))}
-            </div>
-          </div>
-        ))}
-
-        <style>{`
-          .tg-fan-card { transition: border-color 0.15s, background 0.15s; }
-          .tg-fan-card:hover { border-color: ${CLUB_COLOR}55; background: rgba(255,255,255,0.05); }
-        `}</style>
+        <FanGroupDirectory groups={groups} clubColor={CLUB_COLOR} />
 
         <div style={{ marginTop: '48px', padding: '28px', borderRadius: '12px', border: '1px dashed rgba(255,255,255,0.12)', textAlign: 'center' }}>
           <p style={{ fontSize: '15px', fontWeight: 600, marginBottom: '8px' }}>Is your group missing?</p>
diff --git a/app/leeds/page.tsx b/app/leeds/page.tsx
index 3634031..46bdbe2 100644
--- a/app/leeds/page.tsx
+++ b/app/leeds/page.tsx
@@ -2,34 +2,11 @@ import Link from 'next/link';
 import { supabase } from '../../lib/supabase';
 import ClubMapLoader from '../../components/ClubMapLoader';
 import SiteNav from '../../components/SiteNav';
+import FanGroupDirectory, { type FanGroupRow } from '../../components/FanGroupDirectory';
 
 
 export const revalidate = 3600; // refresh from Supabase at most once per hour
 
-type FanGroupRow = {
-  name: string;
-  city: string | null;
-  country: string | null;
-  latitude: number | null;
-  longitude: number | null;
-  url: string;
-  description: string | null;
-  region: string | null;
-  type: string | null;
-};
-
-function typeColor(type: string | null, clubColor: string) {
-  if (type === 'community') return '#FFFFFF';
-  if (type === 'fan_bar') return '#F97316';
-  return clubColor;
-}
-
-function typeLabel(type: string | null) {
-  if (type === 'community') return 'Fan community';
-  if (type === 'fan_bar') return 'Fan bar';
-  return 'Supporter club';
-}
-
 export default async function LeedsPage() {
   const { data: club } = await supabase
     .from('clubs')
@@ -66,20 +43,6 @@ export default async function LeedsPage() {
       description: g.description,
     }));
 
-  const regionMap = new Map<string, FanGroupRow[]>();
-  for (const g of groups) {
-    const region = g.region || 'More worldwide';
-    if (!regionMap.has(region)) regionMap.set(region, []);
-    regionMap.get(region)!.push(g);
-  }
-  const regionOrder = Array.from(regionMap.keys()).sort((a, b) => {
-    if (a === 'United Kingdom') return -1;
-    if (b === 'United Kingdom') return 1;
-    if (a === 'More worldwide') return 1;
-    if (b === 'More worldwide') return -1;
-    return a.localeCompare(b);
-  });
-
   return (
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
@@ -120,40 +83,7 @@ export default async function LeedsPage() {
         <p style={{ color: 'rgba(255,255,255,0.4)', fontSize: '14px', marginBottom: '48px' }}>
           Official supporter clubs and fan communities worldwide. Click any group to visit their site.
         </p>
-        {regionOrder.map(region => (
-          <div key={region} style={{ marginBottom: '48px' }}>
-            <h3 style={{ fontSize: '13px', fontWeight: 600, letterSpacing: '0.1em', textTransform: 'uppercase', color: CLUB_COLOR, marginBottom: '16px', paddingBottom: '8px', borderBottom: `1px solid rgba(108,171,221,0.2)` }}>
-              {region}
-            </h3>
-            <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
-              {regionMap.get(region)!.map(group => (
-                <a key={group.name} href={group.url} target="_blank" rel="noopener noreferrer" className="tg-fan-card"
-                  style={{ display: 'block', textDecoration: 'none', background: 'rgba(255,255,255,0.03)', border: '1px solid rgba(255,255,255,0.07)', borderRadius: '10px', padding: '16px 20px' }}
-                >
-                  <div style={{ display: 'flex', alignItems: 'flex-start', justifyContent: 'space-between', gap: '12px', flexWrap: 'wrap' }}>
-                    <div style={{ flex: 1, minWidth: '200px' }}>
-                      <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '6px' }}>
-                        <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: typeColor(group.type, CLUB_COLOR), flexShrink: 0 }} />
-                        <span style={{ fontSize: '15px', fontWeight: 600, color: '#fff' }}>{group.name}</span>
-                        <span style={{ fontSize: '10px', fontWeight: 500, letterSpacing: '0.06em', textTransform: 'uppercase', color: typeColor(group.type, CLUB_COLOR), opacity: 0.8 }}>{typeLabel(group.type)}</span>
-                      </div>
-                      <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.35)', marginBottom: '6px' }}>{group.city ? `${group.city} · ${group.country}` : group.country}</div>
-                      {group.description && (
-                        <div style={{ fontSize: '13px', color: 'rgba(255,255,255,0.55)', lineHeight: 1.5 }}>{group.description}</div>
-                      )}
-                    </div>
-                    <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.25)', flexShrink: 0, paddingTop: '2px' }}>Visit →</div>
-                  </div>
-                </a>
-              ))}
-            </div>
-          </div>
-        ))}
-
-        <style>{`
-          .tg-fan-card { transition: border-color 0.15s, background 0.15s; }
-          .tg-fan-card:hover { border-color: ${CLUB_COLOR}55; background: rgba(255,255,255,0.05); }
-        `}</style>
+        <FanGroupDirectory groups={groups} clubColor={CLUB_COLOR} />
 
         <div style={{ marginTop: '48px', padding: '28px', borderRadius: '12px', border: '1px dashed rgba(255,255,255,0.12)', textAlign: 'center' }}>
           <p style={{ fontSize: '15px', fontWeight: 600, marginBottom: '8px' }}>Is your group missing?</p>
diff --git a/app/liverpool/page.tsx b/app/liverpool/page.tsx
index c381053..d4894b7 100644
--- a/app/liverpool/page.tsx
+++ b/app/liverpool/page.tsx
@@ -2,34 +2,11 @@ import Link from 'next/link';
 import { supabase } from '../../lib/supabase';
 import ClubMapLoader from '../../components/ClubMapLoader';
 import SiteNav from '../../components/SiteNav';
+import FanGroupDirectory, { type FanGroupRow } from '../../components/FanGroupDirectory';
 
 
 export const revalidate = 3600; // refresh from Supabase at most once per hour
 
-type FanGroupRow = {
-  name: string;
-  city: string | null;
-  country: string | null;
-  latitude: number | null;
-  longitude: number | null;
-  url: string;
-  description: string | null;
-  region: string | null;
-  type: string | null;
-};
-
-function typeColor(type: string | null, clubColor: string) {
-  if (type === 'community') return '#FFFFFF';
-  if (type === 'fan_bar') return '#F97316';
-  return clubColor;
-}
-
-function typeLabel(type: string | null) {
-  if (type === 'community') return 'Fan community';
-  if (type === 'fan_bar') return 'Fan bar';
-  return 'Supporter club';
-}
-
 export default async function LiverpoolPage() {
   const { data: club } = await supabase
     .from('clubs')
@@ -66,20 +43,6 @@ export default async function LiverpoolPage() {
       description: g.description,
     }));
 
-  const regionMap = new Map<string, FanGroupRow[]>();
-  for (const g of groups) {
-    const region = g.region || 'More worldwide';
-    if (!regionMap.has(region)) regionMap.set(region, []);
-    regionMap.get(region)!.push(g);
-  }
-  const regionOrder = Array.from(regionMap.keys()).sort((a, b) => {
-    if (a === 'United Kingdom') return -1;
-    if (b === 'United Kingdom') return 1;
-    if (a === 'More worldwide') return 1;
-    if (b === 'More worldwide') return -1;
-    return a.localeCompare(b);
-  });
-
   return (
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
@@ -116,40 +79,7 @@ export default async function LiverpoolPage() {
         <p style={{ color: 'rgba(255,255,255,0.4)', fontSize: '14px', marginBottom: '48px' }}>
           Official supporter clubs, fan communities and bars worldwide. Click any group to visit their site.
         </p>
-        {regionOrder.map(region => (
-          <div key={region} style={{ marginBottom: '48px' }}>
-            <h3 style={{ fontSize: '13px', fontWeight: 600, letterSpacing: '0.1em', textTransform: 'uppercase', color: CLUB_COLOR, marginBottom: '16px', paddingBottom: '8px', borderBottom: `1px solid rgba(108,171,221,0.2)` }}>
-              {region}
-            </h3>
-            <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
-              {regionMap.get(region)!.map(group => (
-                <a key={group.name} href={group.url} target="_blank" rel="noopener noreferrer" className="tg-fan-card"
-                  style={{ display: 'block', textDecoration: 'none', background: 'rgba(255,255,255,0.03)', border: '1px solid rgba(255,255,255,0.07)', borderRadius: '10px', padding: '16px 20px' }}
-                >
-                  <div style={{ display: 'flex', alignItems: 'flex-start', justifyContent: 'space-between', gap: '12px', flexWrap: 'wrap' }}>
-                    <div style={{ flex: 1, minWidth: '200px' }}>
-                      <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '6px' }}>
-                        <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: typeColor(group.type, CLUB_COLOR), flexShrink: 0 }} />
-                        <span style={{ fontSize: '15px', fontWeight: 600, color: '#fff' }}>{group.name}</span>
-                        <span style={{ fontSize: '10px', fontWeight: 500, letterSpacing: '0.06em', textTransform: 'uppercase', color: typeColor(group.type, CLUB_COLOR), opacity: 0.8 }}>{typeLabel(group.type)}</span>
-                      </div>
-                      <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.35)', marginBottom: '6px' }}>{group.city ? `${group.city} · ${group.country}` : group.country}</div>
-                      {group.description && (
-                        <div style={{ fontSize: '13px', color: 'rgba(255,255,255,0.55)', lineHeight: 1.5 }}>{group.description}</div>
-                      )}
-                    </div>
-                    <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.25)', flexShrink: 0, paddingTop: '2px' }}>Visit →</div>
-                  </div>
-                </a>
-              ))}
-            </div>
-          </div>
-        ))}
-
-        <style>{`
-          .tg-fan-card { transition: border-color 0.15s, background 0.15s; }
-          .tg-fan-card:hover { border-color: ${CLUB_COLOR}55; background: rgba(255,255,255,0.05); }
-        `}</style>
+        <FanGroupDirectory groups={groups} clubColor={CLUB_COLOR} />
 
         <div style={{ marginTop: '48px', padding: '28px', borderRadius: '12px', border: '1px dashed rgba(255,255,255,0.12)', textAlign: 'center' }}>
           <p style={{ fontSize: '15px', fontWeight: 600, marginBottom: '8px' }}>Is your group missing?</p>
diff --git a/app/manchester-city/page.tsx b/app/manchester-city/page.tsx
index 8831fbf..ac6688a 100644
--- a/app/manchester-city/page.tsx
+++ b/app/manchester-city/page.tsx
@@ -2,33 +2,10 @@ import Link from 'next/link';
 import { supabase } from '../../lib/supabase';
 import ClubMapLoader from '../../components/ClubMapLoader';
 import SiteNav from '../../components/SiteNav';
+import FanGroupDirectory, { type FanGroupRow } from '../../components/FanGroupDirectory';
 
 export const revalidate = 3600; // refresh from Supabase at most once per hour
 
-type FanGroupRow = {
-  name: string;
-  city: string | null;
-  country: string | null;
-  latitude: number | null;
-  longitude: number | null;
-  url: string;
-  description: string | null;
-  region: string | null;
-  type: string | null;
-};
-
-function typeColor(type: string | null, clubColor: string) {
-  if (type === 'community') return '#FFFFFF';
-  if (type === 'fan_bar') return '#F97316';
-  return clubColor;
-}
-
-function typeLabel(type: string | null) {
-  if (type === 'community') return 'Fan community';
-  if (type === 'fan_bar') return 'Fan bar';
-  return 'Supporter club';
-}
-
 export default async function ManCityPage() {
   const { data: club } = await supabase
     .from('clubs')
@@ -65,20 +42,6 @@ export default async function ManCityPage() {
       description: g.description,
     }));
 
-  const regionMap = new Map<string, FanGroupRow[]>();
-  for (const g of groups) {
-    const region = g.region || 'More worldwide';
-    if (!regionMap.has(region)) regionMap.set(region, []);
-    regionMap.get(region)!.push(g);
-  }
-  const regionOrder = Array.from(regionMap.keys()).sort((a, b) => {
-    if (a === 'United Kingdom') return -1;
-    if (b === 'United Kingdom') return 1;
-    if (a === 'More worldwide') return 1;
-    if (b === 'More worldwide') return -1;
-    return a.localeCompare(b);
-  });
-
   return (
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
@@ -119,39 +82,7 @@ export default async function ManCityPage() {
         <p style={{ color: 'rgba(255,255,255,0.4)', fontSize: '14px', marginBottom: '48px' }}>
           Official supporter clubs, fan communities and bars worldwide. Click any group to visit their site.
         </p>
-        <style>{`
-          .tg-fan-card { transition: border-color 0.15s, background 0.15s; }
-          .tg-fan-card:hover { border-color: ${CLUB_COLOR}55; background: rgba(255,255,255,0.05); }
-        `}</style>
-        {regionOrder.map(region => (
-          <div key={region} style={{ marginBottom: '48px' }}>
-            <h3 style={{ fontSize: '13px', fontWeight: 600, letterSpacing: '0.1em', textTransform: 'uppercase', color: CLUB_COLOR, marginBottom: '16px', paddingBottom: '8px', borderBottom: `1px solid rgba(108,171,221,0.2)` }}>
-              {region}
-            </h3>
-            <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
-              {regionMap.get(region)!.map(group => (
-                <a key={group.name} href={group.url} target="_blank" rel="noopener noreferrer" className="tg-fan-card"
-                  style={{ display: 'block', textDecoration: 'none', background: 'rgba(255,255,255,0.03)', border: '1px solid rgba(255,255,255,0.07)', borderRadius: '10px', padding: '16px 20px' }}
-                >
-                  <div style={{ display: 'flex', alignItems: 'flex-start', justifyContent: 'space-between', gap: '12px', flexWrap: 'wrap' }}>
-                    <div style={{ flex: 1, minWidth: '200px' }}>
-                      <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '6px' }}>
-                        <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: typeColor(group.type, CLUB_COLOR), flexShrink: 0 }} />
-                        <span style={{ fontSize: '15px', fontWeight: 600, color: '#fff' }}>{group.name}</span>
-                        <span style={{ fontSize: '10px', fontWeight: 500, letterSpacing: '0.06em', textTransform: 'uppercase', color: typeColor(group.type, CLUB_COLOR), opacity: 0.8 }}>{typeLabel(group.type)}</span>
-                      </div>
-                      <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.35)', marginBottom: '6px' }}>{group.city ? `${group.city} · ${group.country}` : group.country}</div>
-                      {group.description && (
-                        <div style={{ fontSize: '13px', color: 'rgba(255,255,255,0.55)', lineHeight: 1.5 }}>{group.description}</div>
-                      )}
-                    </div>
-                    <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.25)', flexShrink: 0, paddingTop: '2px' }}>Visit →</div>
-                  </div>
-                </a>
-              ))}
-            </div>
-          </div>
-        ))}
+        <FanGroupDirectory groups={groups} clubColor={CLUB_COLOR} />
 
         {/* Missing group CTA */}
         <div style={{ marginTop: '48px', padding: '28px', borderRadius: '12px', border: '1px dashed rgba(255,255,255,0.12)', textAlign: 'center' }}>
diff --git a/app/manchester-united/page.tsx b/app/manchester-united/page.tsx
index 07c6738..9f34095 100644
--- a/app/manchester-united/page.tsx
+++ b/app/manchester-united/page.tsx
@@ -2,34 +2,11 @@ import Link from 'next/link';
 import { supabase } from '../../lib/supabase';
 import ClubMapLoader from '../../components/ClubMapLoader';
 import SiteNav from '../../components/SiteNav';
+import FanGroupDirectory, { type FanGroupRow } from '../../components/FanGroupDirectory';
 
 
 export const revalidate = 3600; // refresh from Supabase at most once per hour
 
-type FanGroupRow = {
-  name: string;
-  city: string | null;
-  country: string | null;
-  latitude: number | null;
-  longitude: number | null;
-  url: string;
-  description: string | null;
-  region: string | null;
-  type: string | null;
-};
-
-function typeColor(type: string | null, clubColor: string) {
-  if (type === 'community') return '#FFFFFF';
-  if (type === 'fan_bar') return '#F97316';
-  return clubColor;
-}
-
-function typeLabel(type: string | null) {
-  if (type === 'community') return 'Fan community';
-  if (type === 'fan_bar') return 'Fan bar';
-  return 'Supporter club';
-}
-
 export default async function ManUtdPage() {
   const { data: club } = await supabase
     .from('clubs')
@@ -66,20 +43,6 @@ export default async function ManUtdPage() {
       description: g.description,
     }));
 
-  const regionMap = new Map<string, FanGroupRow[]>();
-  for (const g of groups) {
-    const region = g.region || 'More worldwide';
-    if (!regionMap.has(region)) regionMap.set(region, []);
-    regionMap.get(region)!.push(g);
-  }
-  const regionOrder = Array.from(regionMap.keys()).sort((a, b) => {
-    if (a === 'United Kingdom') return -1;
-    if (b === 'United Kingdom') return 1;
-    if (a === 'More worldwide') return 1;
-    if (b === 'More worldwide') return -1;
-    return a.localeCompare(b);
-  });
-
   return (
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
@@ -120,40 +83,7 @@ export default async function ManUtdPage() {
         <p style={{ color: 'rgba(255,255,255,0.4)', fontSize: '14px', marginBottom: '48px' }}>
           Official supporter clubs and fan communities worldwide. Click any group to visit their site.
         </p>
-        {regionOrder.map(region => (
-          <div key={region} style={{ marginBottom: '48px' }}>
-            <h3 style={{ fontSize: '13px', fontWeight: 600, letterSpacing: '0.1em', textTransform: 'uppercase', color: CLUB_COLOR, marginBottom: '16px', paddingBottom: '8px', borderBottom: `1px solid rgba(108,171,221,0.2)` }}>
-              {region}
-            </h3>
-            <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
-              {regionMap.get(region)!.map(group => (
-                <a key={group.name} href={group.url} target="_blank" rel="noopener noreferrer" className="tg-fan-card"
-                  style={{ display: 'block', textDecoration: 'none', background: 'rgba(255,255,255,0.03)', border: '1px solid rgba(255,255,255,0.07)', borderRadius: '10px', padding: '16px 20px' }}
-                >
-                  <div style={{ display: 'flex', alignItems: 'flex-start', justifyContent: 'space-between', gap: '12px', flexWrap: 'wrap' }}>
-                    <div style={{ flex: 1, minWidth: '200px' }}>
-                      <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '6px' }}>
-                        <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: typeColor(group.type, CLUB_COLOR), flexShrink: 0 }} />
-                        <span style={{ fontSize: '15px', fontWeight: 600, color: '#fff' }}>{group.name}</span>
-                        <span style={{ fontSize: '10px', fontWeight: 500, letterSpacing: '0.06em', textTransform: 'uppercase', color: typeColor(group.type, CLUB_COLOR), opacity: 0.8 }}>{typeLabel(group.type)}</span>
-                      </div>
-                      <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.35)', marginBottom: '6px' }}>{group.city ? `${group.city} · ${group.country}` : group.country}</div>
-                      {group.description && (
-                        <div style={{ fontSize: '13px', color: 'rgba(255,255,255,0.55)', lineHeight: 1.5 }}>{group.description}</div>
-                      )}
-                    </div>
-                    <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.25)', flexShrink: 0, paddingTop: '2px' }}>Visit →</div>
-                  </div>
-                </a>
-              ))}
-            </div>
-          </div>
-        ))}
-
-        <style>{`
-          .tg-fan-card { transition: border-color 0.15s, background 0.15s; }
-          .tg-fan-card:hover { border-color: ${CLUB_COLOR}55; background: rgba(255,255,255,0.05); }
-        `}</style>
+        <FanGroupDirectory groups={groups} clubColor={CLUB_COLOR} />
 
         <div style={{ marginTop: '48px', padding: '28px', borderRadius: '12px', border: '1px dashed rgba(255,255,255,0.12)', textAlign: 'center' }}>
           <p style={{ fontSize: '15px', fontWeight: 600, marginBottom: '8px' }}>Is your group missing?</p>
diff --git a/app/newcastle/page.tsx b/app/newcastle/page.tsx
index db55bfa..0ce27a7 100644
--- a/app/newcastle/page.tsx
+++ b/app/newcastle/page.tsx
@@ -2,6 +2,7 @@ import Link from 'next/link';
 import { supabase } from '../../lib/supabase';
 import ClubMapLoader from '../../components/ClubMapLoader';
 import SiteNav from '../../components/SiteNav';
+import FanGroupDirectory, { type FanGroupRow } from '../../components/FanGroupDirectory';
 
 
 const CLUB_COLOR = '#241F20';
@@ -9,30 +10,6 @@ const CLUB_COLOR_LIGHT = '#8B8B8B';
 
 export const revalidate = 3600; // refresh from Supabase at most once per hour
 
-type FanGroupRow = {
-  name: string;
-  city: string | null;
-  country: string | null;
-  latitude: number | null;
-  longitude: number | null;
-  url: string;
-  description: string | null;
-  region: string | null;
-  type: string | null;
-};
-
-function typeColor(type: string | null, clubColor: string) {
-  if (type === 'community') return '#FFFFFF';
-  if (type === 'fan_bar') return '#F97316';
-  return clubColor;
-}
-
-function typeLabel(type: string | null) {
-  if (type === 'community') return 'Fan community';
-  if (type === 'fan_bar') return 'Fan bar';
-  return 'Supporter club';
-}
-
 export default async function NewcastlePage() {
   const { data: club } = await supabase
     .from('clubs')
@@ -69,20 +46,6 @@ export default async function NewcastlePage() {
       description: g.description,
     }));
 
-  const regionMap = new Map<string, FanGroupRow[]>();
-  for (const g of groups) {
-    const region = g.region || 'More worldwide';
-    if (!regionMap.has(region)) regionMap.set(region, []);
-    regionMap.get(region)!.push(g);
-  }
-  const regionOrder = Array.from(regionMap.keys()).sort((a, b) => {
-    if (a === 'United Kingdom') return -1;
-    if (b === 'United Kingdom') return 1;
-    if (a === 'More worldwide') return 1;
-    if (b === 'More worldwide') return -1;
-    return a.localeCompare(b);
-  });
-
   return (
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
@@ -123,40 +86,7 @@ export default async function NewcastlePage() {
         <p style={{ color: 'rgba(255,255,255,0.4)', fontSize: '14px', marginBottom: '48px' }}>
           Official supporter clubs and fan communities worldwide. Click any group to visit their site.
         </p>
-        {regionOrder.map(region => (
-          <div key={region} style={{ marginBottom: '48px' }}>
-            <h3 style={{ fontSize: '13px', fontWeight: 600, letterSpacing: '0.1em', textTransform: 'uppercase', color: CLUB_COLOR_LIGHT, marginBottom: '16px', paddingBottom: '8px', borderBottom: `1px solid rgba(139,139,139,0.2)` }}>
-              {region}
-            </h3>
-            <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
-              {regionMap.get(region)!.map(group => (
-                <a key={group.name} href={group.url} target="_blank" rel="noopener noreferrer" className="tg-fan-card"
-                  style={{ display: 'block', textDecoration: 'none', background: 'rgba(255,255,255,0.03)', border: '1px solid rgba(255,255,255,0.07)', borderRadius: '10px', padding: '16px 20px' }}
-                >
-                  <div style={{ display: 'flex', alignItems: 'flex-start', justifyContent: 'space-between', gap: '12px', flexWrap: 'wrap' }}>
-                    <div style={{ flex: 1, minWidth: '200px' }}>
-                      <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '6px' }}>
-                        <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: typeColor(group.type, CLUB_COLOR_LIGHT), flexShrink: 0 }} />
-                        <span style={{ fontSize: '15px', fontWeight: 600, color: '#fff' }}>{group.name}</span>
-                        <span style={{ fontSize: '10px', fontWeight: 500, letterSpacing: '0.06em', textTransform: 'uppercase', color: typeColor(group.type, CLUB_COLOR_LIGHT), opacity: 0.8 }}>{typeLabel(group.type)}</span>
-                      </div>
-                      <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.35)', marginBottom: '6px' }}>{group.city ? `${group.city} · ${group.country}` : group.country}</div>
-                      {group.description && (
-                        <div style={{ fontSize: '13px', color: 'rgba(255,255,255,0.55)', lineHeight: 1.5 }}>{group.description}</div>
-                      )}
-                    </div>
-                    <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.25)', flexShrink: 0, paddingTop: '2px' }}>Visit →</div>
-                  </div>
-                </a>
-              ))}
-            </div>
-          </div>
-        ))}
-
-        <style>{`
-          .tg-fan-card { transition: border-color 0.15s, background 0.15s; }
-          .tg-fan-card:hover { border-color: ${CLUB_COLOR_LIGHT}55; background: rgba(255,255,255,0.05); }
-        `}</style>
+        <FanGroupDirectory groups={groups} clubColor={CLUB_COLOR_LIGHT} />
 
         <div style={{ marginTop: '48px', padding: '28px', borderRadius: '12px', border: '1px dashed rgba(255,255,255,0.12)', textAlign: 'center' }}>
           <p style={{ fontSize: '15px', fontWeight: 600, marginBottom: '8px' }}>Is your group missing?</p>
diff --git a/app/nottingham-forest/page.tsx b/app/nottingham-forest/page.tsx
index 6dd442e..9eb4384 100644
--- a/app/nottingham-forest/page.tsx
+++ b/app/nottingham-forest/page.tsx
@@ -2,34 +2,11 @@ import Link from 'next/link';
 import { supabase } from '../../lib/supabase';
 import ClubMapLoader from '../../components/ClubMapLoader';
 import SiteNav from '../../components/SiteNav';
+import FanGroupDirectory, { type FanGroupRow } from '../../components/FanGroupDirectory';
 
 
 export const revalidate = 3600; // refresh from Supabase at most once per hour
 
-type FanGroupRow = {
-  name: string;
-  city: string | null;
-  country: string | null;
-  latitude: number | null;
-  longitude: number | null;
-  url: string;
-  description: string | null;
-  region: string | null;
-  type: string | null;
-};
-
-function typeColor(type: string | null, clubColor: string) {
-  if (type === 'community') return '#FFFFFF';
-  if (type === 'fan_bar') return '#F97316';
-  return clubColor;
-}
-
-function typeLabel(type: string | null) {
-  if (type === 'community') return 'Fan community';
-  if (type === 'fan_bar') return 'Fan bar';
-  return 'Supporter club';
-}
-
 export default async function NottinghamForestPage() {
   const { data: club } = await supabase
     .from('clubs')
@@ -66,20 +43,6 @@ export default async function NottinghamForestPage() {
       description: g.description,
     }));
 
-  const regionMap = new Map<string, FanGroupRow[]>();
-  for (const g of groups) {
-    const region = g.region || 'More worldwide';
-    if (!regionMap.has(region)) regionMap.set(region, []);
-    regionMap.get(region)!.push(g);
-  }
-  const regionOrder = Array.from(regionMap.keys()).sort((a, b) => {
-    if (a === 'United Kingdom') return -1;
-    if (b === 'United Kingdom') return 1;
-    if (a === 'More worldwide') return 1;
-    if (b === 'More worldwide') return -1;
-    return a.localeCompare(b);
-  });
-
   return (
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
@@ -120,40 +83,7 @@ export default async function NottinghamForestPage() {
         <p style={{ color: 'rgba(255,255,255,0.4)', fontSize: '14px', marginBottom: '48px' }}>
           Official supporter clubs and fan communities worldwide. Click any group to visit their site.
         </p>
-        {regionOrder.map(region => (
-          <div key={region} style={{ marginBottom: '48px' }}>
-            <h3 style={{ fontSize: '13px', fontWeight: 600, letterSpacing: '0.1em', textTransform: 'uppercase', color: CLUB_COLOR, marginBottom: '16px', paddingBottom: '8px', borderBottom: `1px solid rgba(108,171,221,0.2)` }}>
-              {region}
-            </h3>
-            <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
-              {regionMap.get(region)!.map(group => (
-                <a key={group.name} href={group.url} target="_blank" rel="noopener noreferrer" className="tg-fan-card"
-                  style={{ display: 'block', textDecoration: 'none', background: 'rgba(255,255,255,0.03)', border: '1px solid rgba(255,255,255,0.07)', borderRadius: '10px', padding: '16px 20px' }}
-                >
-                  <div style={{ display: 'flex', alignItems: 'flex-start', justifyContent: 'space-between', gap: '12px', flexWrap: 'wrap' }}>
-                    <div style={{ flex: 1, minWidth: '200px' }}>
-                      <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '6px' }}>
-                        <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: typeColor(group.type, CLUB_COLOR), flexShrink: 0 }} />
-                        <span style={{ fontSize: '15px', fontWeight: 600, color: '#fff' }}>{group.name}</span>
-                        <span style={{ fontSize: '10px', fontWeight: 500, letterSpacing: '0.06em', textTransform: 'uppercase', color: typeColor(group.type, CLUB_COLOR), opacity: 0.8 }}>{typeLabel(group.type)}</span>
-                      </div>
-                      <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.35)', marginBottom: '6px' }}>{group.city ? `${group.city} · ${group.country}` : group.country}</div>
-                      {group.description && (
-                        <div style={{ fontSize: '13px', color: 'rgba(255,255,255,0.55)', lineHeight: 1.5 }}>{group.description}</div>
-                      )}
-                    </div>
-                    <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.25)', flexShrink: 0, paddingTop: '2px' }}>Visit →</div>
-                  </div>
-                </a>
-              ))}
-            </div>
-          </div>
-        ))}
-
-        <style>{`
-          .tg-fan-card { transition: border-color 0.15s, background 0.15s; }
-          .tg-fan-card:hover { border-color: ${CLUB_COLOR}55; background: rgba(255,255,255,0.05); }
-        `}</style>
+        <FanGroupDirectory groups={groups} clubColor={CLUB_COLOR} />
 
         <div style={{ marginTop: '48px', padding: '28px', borderRadius: '12px', border: '1px dashed rgba(255,255,255,0.12)', textAlign: 'center' }}>
           <p style={{ fontSize: '15px', fontWeight: 600, marginBottom: '8px' }}>Is your group missing?</p>
diff --git a/app/rangers/page.tsx b/app/rangers/page.tsx
index afd9ccf..8291ab7 100644
--- a/app/rangers/page.tsx
+++ b/app/rangers/page.tsx
@@ -2,34 +2,11 @@ import Link from 'next/link';
 import { supabase } from '../../lib/supabase';
 import ClubMapLoader from '../../components/ClubMapLoader';
 import SiteNav from '../../components/SiteNav';
+import FanGroupDirectory, { type FanGroupRow } from '../../components/FanGroupDirectory';
 
 
 export const revalidate = 3600; // refresh from Supabase at most once per hour
 
-type FanGroupRow = {
-  name: string;
-  city: string | null;
-  country: string | null;
-  latitude: number | null;
-  longitude: number | null;
-  url: string;
-  description: string | null;
-  region: string | null;
-  type: string | null;
-};
-
-function typeColor(type: string | null, clubColor: string) {
-  if (type === 'community') return '#FFFFFF';
-  if (type === 'fan_bar') return '#F97316';
-  return clubColor;
-}
-
-function typeLabel(type: string | null) {
-  if (type === 'community') return 'Fan community';
-  if (type === 'fan_bar') return 'Fan bar';
-  return 'Supporter club';
-}
-
 export default async function RangersPage() {
   const { data: club } = await supabase
     .from('clubs')
@@ -66,20 +43,6 @@ export default async function RangersPage() {
       description: g.description,
     }));
 
-  const regionMap = new Map<string, FanGroupRow[]>();
-  for (const g of groups) {
-    const region = g.region || 'More worldwide';
-    if (!regionMap.has(region)) regionMap.set(region, []);
-    regionMap.get(region)!.push(g);
-  }
-  const regionOrder = Array.from(regionMap.keys()).sort((a, b) => {
-    if (a === 'United Kingdom') return -1;
-    if (b === 'United Kingdom') return 1;
-    if (a === 'More worldwide') return 1;
-    if (b === 'More worldwide') return -1;
-    return a.localeCompare(b);
-  });
-
   return (
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
@@ -120,40 +83,7 @@ export default async function RangersPage() {
         <p style={{ color: 'rgba(255,255,255,0.4)', fontSize: '14px', marginBottom: '48px' }}>
           Official supporter clubs and fan communities worldwide. Click any group to visit their site.
         </p>
-        {regionOrder.map(region => (
-          <div key={region} style={{ marginBottom: '48px' }}>
-            <h3 style={{ fontSize: '13px', fontWeight: 600, letterSpacing: '0.1em', textTransform: 'uppercase', color: CLUB_COLOR, marginBottom: '16px', paddingBottom: '8px', borderBottom: `1px solid rgba(108,171,221,0.2)` }}>
-              {region}
-            </h3>
-            <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
-              {regionMap.get(region)!.map(group => (
-                <a key={group.name} href={group.url} target="_blank" rel="noopener noreferrer" className="tg-fan-card"
-                  style={{ display: 'block', textDecoration: 'none', background: 'rgba(255,255,255,0.03)', border: '1px solid rgba(255,255,255,0.07)', borderRadius: '10px', padding: '16px 20px' }}
-                >
-                  <div style={{ display: 'flex', alignItems: 'flex-start', justifyContent: 'space-between', gap: '12px', flexWrap: 'wrap' }}>
-                    <div style={{ flex: 1, minWidth: '200px' }}>
-                      <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '6px' }}>
-                        <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: typeColor(group.type, CLUB_COLOR), flexShrink: 0 }} />
-                        <span style={{ fontSize: '15px', fontWeight: 600, color: '#fff' }}>{group.name}</span>
-                        <span style={{ fontSize: '10px', fontWeight: 500, letterSpacing: '0.06em', textTransform: 'uppercase', color: typeColor(group.type, CLUB_COLOR), opacity: 0.8 }}>{typeLabel(group.type)}</span>
-                      </div>
-                      <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.35)', marginBottom: '6px' }}>{group.city ? `${group.city} · ${group.country}` : group.country}</div>
-                      {group.description && (
-                        <div style={{ fontSize: '13px', color: 'rgba(255,255,255,0.55)', lineHeight: 1.5 }}>{group.description}</div>
-                      )}
-                    </div>
-                    <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.25)', flexShrink: 0, paddingTop: '2px' }}>Visit →</div>
-                  </div>
-                </a>
-              ))}
-            </div>
-          </div>
-        ))}
-
-        <style>{`
-          .tg-fan-card { transition: border-color 0.15s, background 0.15s; }
-          .tg-fan-card:hover { border-color: ${CLUB_COLOR}55; background: rgba(255,255,255,0.05); }
-        `}</style>
+        <FanGroupDirectory groups={groups} clubColor={CLUB_COLOR} />
 
         <div style={{ marginTop: '48px', padding: '28px', borderRadius: '12px', border: '1px dashed rgba(255,255,255,0.12)', textAlign: 'center' }}>
           <p style={{ fontSize: '15px', fontWeight: 600, marginBottom: '8px' }}>Is your group missing?</p>
diff --git a/app/spurs/page.tsx b/app/spurs/page.tsx
index 54f90d7..58b8a08 100644
--- a/app/spurs/page.tsx
+++ b/app/spurs/page.tsx
@@ -2,34 +2,11 @@ import Link from 'next/link';
 import { supabase } from '../../lib/supabase';
 import ClubMapLoader from '../../components/ClubMapLoader';
 import SiteNav from '../../components/SiteNav';
+import FanGroupDirectory, { type FanGroupRow } from '../../components/FanGroupDirectory';
 
 
 export const revalidate = 3600; // refresh from Supabase at most once per hour
 
-type FanGroupRow = {
-  name: string;
-  city: string | null;
-  country: string | null;
-  latitude: number | null;
-  longitude: number | null;
-  url: string;
-  description: string | null;
-  region: string | null;
-  type: string | null;
-};
-
-function typeColor(type: string | null, clubColor: string) {
-  if (type === 'community') return '#FFFFFF';
-  if (type === 'fan_bar') return '#F97316';
-  return clubColor;
-}
-
-function typeLabel(type: string | null) {
-  if (type === 'community') return 'Fan community';
-  if (type === 'fan_bar') return 'Fan bar';
-  return 'Supporter club';
-}
-
 export default async function SpursPage() {
   const { data: club } = await supabase
     .from('clubs')
@@ -66,20 +43,6 @@ export default async function SpursPage() {
       description: g.description,
     }));
 
-  const regionMap = new Map<string, FanGroupRow[]>();
-  for (const g of groups) {
-    const region = g.region || 'More worldwide';
-    if (!regionMap.has(region)) regionMap.set(region, []);
-    regionMap.get(region)!.push(g);
-  }
-  const regionOrder = Array.from(regionMap.keys()).sort((a, b) => {
-    if (a === 'United Kingdom') return -1;
-    if (b === 'United Kingdom') return 1;
-    if (a === 'More worldwide') return 1;
-    if (b === 'More worldwide') return -1;
-    return a.localeCompare(b);
-  });
-
   return (
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
@@ -120,40 +83,7 @@ export default async function SpursPage() {
         <p style={{ color: 'rgba(255,255,255,0.4)', fontSize: '14px', marginBottom: '48px' }}>
           Official supporter clubs and fan communities worldwide. Click any group to visit their site.
         </p>
-        {regionOrder.map(region => (
-          <div key={region} style={{ marginBottom: '48px' }}>
-            <h3 style={{ fontSize: '13px', fontWeight: 600, letterSpacing: '0.1em', textTransform: 'uppercase', color: CLUB_COLOR, marginBottom: '16px', paddingBottom: '8px', borderBottom: `1px solid rgba(108,171,221,0.2)` }}>
-              {region}
-            </h3>
-            <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
-              {regionMap.get(region)!.map(group => (
-                <a key={group.name} href={group.url} target="_blank" rel="noopener noreferrer" className="tg-fan-card"
-                  style={{ display: 'block', textDecoration: 'none', background: 'rgba(255,255,255,0.03)', border: '1px solid rgba(255,255,255,0.07)', borderRadius: '10px', padding: '16px 20px' }}
-                >
-                  <div style={{ display: 'flex', alignItems: 'flex-start', justifyContent: 'space-between', gap: '12px', flexWrap: 'wrap' }}>
-                    <div style={{ flex: 1, minWidth: '200px' }}>
-                      <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '6px' }}>
-                        <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: typeColor(group.type, CLUB_COLOR), flexShrink: 0 }} />
-                        <span style={{ fontSize: '15px', fontWeight: 600, color: '#fff' }}>{group.name}</span>
-                        <span style={{ fontSize: '10px', fontWeight: 500, letterSpacing: '0.06em', textTransform: 'uppercase', color: typeColor(group.type, CLUB_COLOR), opacity: 0.8 }}>{typeLabel(group.type)}</span>
-                      </div>
-                      <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.35)', marginBottom: '6px' }}>{group.city ? `${group.city} · ${group.country}` : group.country}</div>
-                      {group.description && (
-                        <div style={{ fontSize: '13px', color: 'rgba(255,255,255,0.55)', lineHeight: 1.5 }}>{group.description}</div>
-                      )}
-                    </div>
-                    <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.25)', flexShrink: 0, paddingTop: '2px' }}>Visit →</div>
-                  </div>
-                </a>
-              ))}
-            </div>
-          </div>
-        ))}
-
-        <style>{`
-          .tg-fan-card { transition: border-color 0.15s, background 0.15s; }
-          .tg-fan-card:hover { border-color: ${CLUB_COLOR}55; background: rgba(255,255,255,0.05); }
-        `}</style>
+        <FanGroupDirectory groups={groups} clubColor={CLUB_COLOR} />
 
         <div style={{ marginTop: '48px', padding: '28px', borderRadius: '12px', border: '1px dashed rgba(255,255,255,0.12)', textAlign: 'center' }}>
           <p style={{ fontSize: '15px', fontWeight: 600, marginBottom: '8px' }}>Is your group missing?</p>
diff --git a/app/sunderland/page.tsx b/app/sunderland/page.tsx
index cc35b13..0f3a1c5 100644
--- a/app/sunderland/page.tsx
+++ b/app/sunderland/page.tsx
@@ -2,34 +2,11 @@ import Link from 'next/link';
 import { supabase } from '../../lib/supabase';
 import ClubMapLoader from '../../components/ClubMapLoader';
 import SiteNav from '../../components/SiteNav';
+import FanGroupDirectory, { type FanGroupRow } from '../../components/FanGroupDirectory';
 
 
 export const revalidate = 3600; // refresh from Supabase at most once per hour
 
-type FanGroupRow = {
-  name: string;
-  city: string | null;
-  country: string | null;
-  latitude: number | null;
-  longitude: number | null;
-  url: string;
-  description: string | null;
-  region: string | null;
-  type: string | null;
-};
-
-function typeColor(type: string | null, clubColor: string) {
-  if (type === 'community') return '#FFFFFF';
-  if (type === 'fan_bar') return '#F97316';
-  return clubColor;
-}
-
-function typeLabel(type: string | null) {
-  if (type === 'community') return 'Fan community';
-  if (type === 'fan_bar') return 'Fan bar';
-  return 'Supporter club';
-}
-
 export default async function SunderlandPage() {
   const { data: club } = await supabase
     .from('clubs')
@@ -66,20 +43,6 @@ export default async function SunderlandPage() {
       description: g.description,
     }));
 
-  const regionMap = new Map<string, FanGroupRow[]>();
-  for (const g of groups) {
-    const region = g.region || 'More worldwide';
-    if (!regionMap.has(region)) regionMap.set(region, []);
-    regionMap.get(region)!.push(g);
-  }
-  const regionOrder = Array.from(regionMap.keys()).sort((a, b) => {
-    if (a === 'United Kingdom') return -1;
-    if (b === 'United Kingdom') return 1;
-    if (a === 'More worldwide') return 1;
-    if (b === 'More worldwide') return -1;
-    return a.localeCompare(b);
-  });
-
   return (
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
@@ -120,40 +83,7 @@ export default async function SunderlandPage() {
         <p style={{ color: 'rgba(255,255,255,0.4)', fontSize: '14px', marginBottom: '48px' }}>
           Official supporter branches and fan communities worldwide. Click any group to visit their site.
         </p>
-        {regionOrder.map(region => (
-          <div key={region} style={{ marginBottom: '48px' }}>
-            <h3 style={{ fontSize: '13px', fontWeight: 600, letterSpacing: '0.1em', textTransform: 'uppercase', color: CLUB_COLOR, marginBottom: '16px', paddingBottom: '8px', borderBottom: `1px solid rgba(108,171,221,0.2)` }}>
-              {region}
-            </h3>
-            <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
-              {regionMap.get(region)!.map(group => (
-                <a key={group.name} href={group.url} target="_blank" rel="noopener noreferrer" className="tg-fan-card"
-                  style={{ display: 'block', textDecoration: 'none', background: 'rgba(255,255,255,0.03)', border: '1px solid rgba(255,255,255,0.07)', borderRadius: '10px', padding: '16px 20px' }}
-                >
-                  <div style={{ display: 'flex', alignItems: 'flex-start', justifyContent: 'space-between', gap: '12px', flexWrap: 'wrap' }}>
-                    <div style={{ flex: 1, minWidth: '200px' }}>
-                      <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '6px' }}>
-                        <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: typeColor(group.type, CLUB_COLOR), flexShrink: 0 }} />
-                        <span style={{ fontSize: '15px', fontWeight: 600, color: '#fff' }}>{group.name}</span>
-                        <span style={{ fontSize: '10px', fontWeight: 500, letterSpacing: '0.06em', textTransform: 'uppercase', color: typeColor(group.type, CLUB_COLOR), opacity: 0.8 }}>{typeLabel(group.type)}</span>
-                      </div>
-                      <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.35)', marginBottom: '6px' }}>{group.city ? `${group.city} · ${group.country}` : group.country}</div>
-                      {group.description && (
-                        <div style={{ fontSize: '13px', color: 'rgba(255,255,255,0.55)', lineHeight: 1.5 }}>{group.description}</div>
-                      )}
-                    </div>
-                    <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.25)', flexShrink: 0, paddingTop: '2px' }}>Visit →</div>
-                  </div>
-                </a>
-              ))}
-            </div>
-          </div>
-        ))}
-
-        <style>{`
-          .tg-fan-card { transition: border-color 0.15s, background 0.15s; }
-          .tg-fan-card:hover { border-color: ${CLUB_COLOR}55; background: rgba(255,255,255,0.05); }
-        `}</style>
+        <FanGroupDirectory groups={groups} clubColor={CLUB_COLOR} />
 
         <div style={{ marginTop: '48px', padding: '28px', borderRadius: '12px', border: '1px dashed rgba(255,255,255,0.12)', textAlign: 'center' }}>
           <p style={{ fontSize: '15px', fontWeight: 600, marginBottom: '8px' }}>Is your group missing?</p>
