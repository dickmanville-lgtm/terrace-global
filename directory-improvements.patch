diff --git a/app/arsenal/page.tsx b/app/arsenal/page.tsx
index 250380f..9b2e476 100644
--- a/app/arsenal/page.tsx
+++ b/app/arsenal/page.tsx
@@ -27,7 +27,7 @@ export default async function ArsenalPage() {
 
   const { data: groupsData } = await supabase
     .from('fan_groups')
-    .select('name, city, country, latitude, longitude, url, description, region, type')
+    .select('name, city, country, latitude, longitude, url, description, region, type, instagram_url, facebook_url, tiktok_url')
     .eq('club_id', club.id);
 
   const groups: FanGroupRow[] = groupsData || [];
@@ -47,7 +47,7 @@ export default async function ArsenalPage() {
   return (
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
-      <SiteNav active="club-map" club={{ name: 'Arsenal', color: CLUB_COLOR, tagline: 'The Gunners · Est. 1886' }} />
+      <SiteNav active="supporter-groups" club={{ name: 'Arsenal', color: CLUB_COLOR, tagline: 'The Gunners · Est. 1886' }} />
 
       {/* ── Hero ── */}
       <section style={{
diff --git a/app/aston-villa/page.tsx b/app/aston-villa/page.tsx
index e8d80a1..46507f1 100644
--- a/app/aston-villa/page.tsx
+++ b/app/aston-villa/page.tsx
@@ -26,7 +26,7 @@ export default async function AstonVillaPage() {
 
   const { data: groupsData } = await supabase
     .from('fan_groups')
-    .select('name, city, country, latitude, longitude, url, description, region, type')
+    .select('name, city, country, latitude, longitude, url, description, region, type, instagram_url, facebook_url, tiktok_url')
     .eq('club_id', club.id);
 
   const groups: FanGroupRow[] = groupsData || [];
@@ -47,7 +47,7 @@ export default async function AstonVillaPage() {
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
       {/* Nav */}
-      <SiteNav active="club-map" club={{ name: 'Aston Villa', color: CLUB_COLOR, tagline: 'The Villans · Est. 1874' }} />
+      <SiteNav active="supporter-groups" club={{ name: 'Aston Villa', color: CLUB_COLOR, tagline: 'The Villans · Est. 1874' }} />
 
       {/* Hero */}
       <section style={{ padding: '60px 32px 48px', borderBottom: '1px solid rgba(255,255,255,0.06)', background: `linear-gradient(180deg, rgba(149,191,229,0.06) 0%, transparent 100%)` }}>
diff --git a/app/bournemouth/page.tsx b/app/bournemouth/page.tsx
index 9702009..fa2ab99 100644
--- a/app/bournemouth/page.tsx
+++ b/app/bournemouth/page.tsx
@@ -26,7 +26,7 @@ export default async function BournemouthPage() {
 
   const { data: groupsData } = await supabase
     .from('fan_groups')
-    .select('name, city, country, latitude, longitude, url, description, region, type')
+    .select('name, city, country, latitude, longitude, url, description, region, type, instagram_url, facebook_url, tiktok_url')
     .eq('club_id', club.id);
 
   const groups: FanGroupRow[] = groupsData || [];
@@ -47,7 +47,7 @@ export default async function BournemouthPage() {
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
       {/* Nav */}
-      <SiteNav active="club-map" club={{ name: 'AFC Bournemouth', color: CLUB_COLOR, tagline: 'The Cherries · Est. 1899' }} />
+      <SiteNav active="supporter-groups" club={{ name: 'AFC Bournemouth', color: CLUB_COLOR, tagline: 'The Cherries · Est. 1899' }} />
 
       {/* Hero */}
       <section style={{ padding: '60px 32px 48px', borderBottom: '1px solid rgba(255,255,255,0.06)', background: `linear-gradient(180deg, rgba(218,41,28,0.06) 0%, transparent 100%)` }}>
diff --git a/app/brentford/page.tsx b/app/brentford/page.tsx
index 2978e62..59d9bcc 100644
--- a/app/brentford/page.tsx
+++ b/app/brentford/page.tsx
@@ -26,7 +26,7 @@ export default async function BrentfordPage() {
 
   const { data: groupsData } = await supabase
     .from('fan_groups')
-    .select('name, city, country, latitude, longitude, url, description, region, type')
+    .select('name, city, country, latitude, longitude, url, description, region, type, instagram_url, facebook_url, tiktok_url')
     .eq('club_id', club.id);
 
   const groups: FanGroupRow[] = groupsData || [];
@@ -47,7 +47,7 @@ export default async function BrentfordPage() {
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
       {/* Nav */}
-      <SiteNav active="club-map" club={{ name: 'Brentford FC', color: CLUB_COLOR, tagline: 'The Bees · Est. 1889' }} />
+      <SiteNav active="supporter-groups" club={{ name: 'Brentford FC', color: CLUB_COLOR, tagline: 'The Bees · Est. 1889' }} />
 
       {/* Hero */}
       <section style={{ padding: '60px 32px 48px', borderBottom: '1px solid rgba(255,255,255,0.06)', background: `linear-gradient(180deg, rgba(227,6,19,0.06) 0%, transparent 100%)` }}>
diff --git a/app/brighton/page.tsx b/app/brighton/page.tsx
index 299c490..a5465a8 100644
--- a/app/brighton/page.tsx
+++ b/app/brighton/page.tsx
@@ -26,7 +26,7 @@ export default async function BrightonPage() {
 
   const { data: groupsData } = await supabase
     .from('fan_groups')
-    .select('name, city, country, latitude, longitude, url, description, region, type')
+    .select('name, city, country, latitude, longitude, url, description, region, type, instagram_url, facebook_url, tiktok_url')
     .eq('club_id', club.id);
 
   const groups: FanGroupRow[] = groupsData || [];
@@ -47,7 +47,7 @@ export default async function BrightonPage() {
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
       {/* Nav */}
-      <SiteNav active="club-map" club={{ name: 'Brighton & Hove Albion', color: CLUB_COLOR, tagline: 'The Seagulls · Est. 1901' }} />
+      <SiteNav active="supporter-groups" club={{ name: 'Brighton & Hove Albion', color: CLUB_COLOR, tagline: 'The Seagulls · Est. 1901' }} />
 
       {/* Hero */}
       <section style={{ padding: '60px 32px 48px', borderBottom: '1px solid rgba(255,255,255,0.06)', background: `linear-gradient(180deg, rgba(0,87,184,0.06) 0%, transparent 100%)` }}>
diff --git a/app/celtic/page.tsx b/app/celtic/page.tsx
index 24bc286..f39e2c2 100644
--- a/app/celtic/page.tsx
+++ b/app/celtic/page.tsx
@@ -26,7 +26,7 @@ export default async function CelticPage() {
 
   const { data: groupsData } = await supabase
     .from('fan_groups')
-    .select('name, city, country, latitude, longitude, url, description, region, type')
+    .select('name, city, country, latitude, longitude, url, description, region, type, instagram_url, facebook_url, tiktok_url')
     .eq('club_id', club.id);
 
   const groups: FanGroupRow[] = groupsData || [];
@@ -47,7 +47,7 @@ export default async function CelticPage() {
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
       {/* Nav */}
-      <SiteNav active="club-map" club={{ name: 'Celtic FC', color: CLUB_COLOR, tagline: 'The Bhoys · Est. 1887' }} />
+      <SiteNav active="supporter-groups" club={{ name: 'Celtic FC', color: CLUB_COLOR, tagline: 'The Bhoys · Est. 1887' }} />
 
       {/* Hero */}
       <section style={{ padding: '60px 32px 48px', borderBottom: '1px solid rgba(255,255,255,0.06)', background: `linear-gradient(180deg, rgba(22,163,74,0.06) 0%, transparent 100%)` }}>
diff --git a/app/chelsea/page.tsx b/app/chelsea/page.tsx
index 6697836..35b446e 100644
--- a/app/chelsea/page.tsx
+++ b/app/chelsea/page.tsx
@@ -26,7 +26,7 @@ export default async function ChelseaPage() {
 
   const { data: groupsData } = await supabase
     .from('fan_groups')
-    .select('name, city, country, latitude, longitude, url, description, region, type')
+    .select('name, city, country, latitude, longitude, url, description, region, type, instagram_url, facebook_url, tiktok_url')
     .eq('club_id', club.id);
 
   const groups: FanGroupRow[] = groupsData || [];
@@ -47,7 +47,7 @@ export default async function ChelseaPage() {
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
       {/* Nav */}
-      <SiteNav active="club-map" club={{ name: 'Chelsea', color: CLUB_COLOR, tagline: 'The Blues · Est. 1905' }} />
+      <SiteNav active="supporter-groups" club={{ name: 'Chelsea', color: CLUB_COLOR, tagline: 'The Blues · Est. 1905' }} />
 
       {/* Hero */}
       <section style={{ padding: '60px 32px 48px', borderBottom: '1px solid rgba(255,255,255,0.06)', background: `linear-gradient(180deg, rgba(3,70,148,0.06) 0%, transparent 100%)` }}>
diff --git a/app/coventry/page.tsx b/app/coventry/page.tsx
index 231d55c..dafb307 100644
--- a/app/coventry/page.tsx
+++ b/app/coventry/page.tsx
@@ -26,7 +26,7 @@ export default async function CoventryPage() {
 
   const { data: groupsData } = await supabase
     .from('fan_groups')
-    .select('name, city, country, latitude, longitude, url, description, region, type')
+    .select('name, city, country, latitude, longitude, url, description, region, type, instagram_url, facebook_url, tiktok_url')
     .eq('club_id', club.id);
 
   const groups: FanGroupRow[] = groupsData || [];
@@ -47,7 +47,7 @@ export default async function CoventryPage() {
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
       {/* Nav */}
-      <SiteNav active="club-map" club={{ name: 'Coventry City', color: CLUB_COLOR, tagline: 'The Sky Blues · Est. 1883' }} />
+      <SiteNav active="supporter-groups" club={{ name: 'Coventry City', color: CLUB_COLOR, tagline: 'The Sky Blues · Est. 1883' }} />
 
       {/* Hero */}
       <section style={{ padding: '60px 32px 48px', borderBottom: '1px solid rgba(255,255,255,0.06)', background: `linear-gradient(180deg, rgba(89,205,237,0.06) 0%, transparent 100%)` }}>
diff --git a/app/crystal-palace/page.tsx b/app/crystal-palace/page.tsx
index 17f5e74..47a1212 100644
--- a/app/crystal-palace/page.tsx
+++ b/app/crystal-palace/page.tsx
@@ -26,7 +26,7 @@ export default async function CrystalPalacePage() {
 
   const { data: groupsData } = await supabase
     .from('fan_groups')
-    .select('name, city, country, latitude, longitude, url, description, region, type')
+    .select('name, city, country, latitude, longitude, url, description, region, type, instagram_url, facebook_url, tiktok_url')
     .eq('club_id', club.id);
 
   const groups: FanGroupRow[] = groupsData || [];
@@ -47,7 +47,7 @@ export default async function CrystalPalacePage() {
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
       {/* Nav */}
-      <SiteNav active="club-map" club={{ name: 'Crystal Palace', color: CLUB_COLOR, tagline: 'The Eagles · Est. 1905' }} />
+      <SiteNav active="supporter-groups" club={{ name: 'Crystal Palace', color: CLUB_COLOR, tagline: 'The Eagles · Est. 1905' }} />
 
       {/* Hero */}
       <section style={{ padding: '60px 32px 48px', borderBottom: '1px solid rgba(255,255,255,0.06)', background: `linear-gradient(180deg, rgba(27,69,143,0.06) 0%, transparent 100%)` }}>
diff --git a/app/everton/page.tsx b/app/everton/page.tsx
index 978853e..df0dc1f 100644
--- a/app/everton/page.tsx
+++ b/app/everton/page.tsx
@@ -26,7 +26,7 @@ export default async function EvertonPage() {
 
   const { data: groupsData } = await supabase
     .from('fan_groups')
-    .select('name, city, country, latitude, longitude, url, description, region, type')
+    .select('name, city, country, latitude, longitude, url, description, region, type, instagram_url, facebook_url, tiktok_url')
     .eq('club_id', club.id);
 
   const groups: FanGroupRow[] = groupsData || [];
@@ -47,7 +47,7 @@ export default async function EvertonPage() {
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
       {/* Nav */}
-      <SiteNav active="club-map" club={{ name: 'Everton FC', color: CLUB_COLOR, tagline: 'The Toffees · Est. 1878' }} />
+      <SiteNav active="supporter-groups" club={{ name: 'Everton FC', color: CLUB_COLOR, tagline: 'The Toffees · Est. 1878' }} />
 
       {/* Hero */}
       <section style={{ padding: '60px 32px 48px', borderBottom: '1px solid rgba(255,255,255,0.06)', background: `linear-gradient(180deg, rgba(0,51,153,0.06) 0%, transparent 100%)` }}>
diff --git a/app/fulham/page.tsx b/app/fulham/page.tsx
index 3cf6e82..0ef337d 100644
--- a/app/fulham/page.tsx
+++ b/app/fulham/page.tsx
@@ -26,7 +26,7 @@ export default async function FulhamPage() {
 
   const { data: groupsData } = await supabase
     .from('fan_groups')
-    .select('name, city, country, latitude, longitude, url, description, region, type')
+    .select('name, city, country, latitude, longitude, url, description, region, type, instagram_url, facebook_url, tiktok_url')
     .eq('club_id', club.id);
 
   const groups: FanGroupRow[] = groupsData || [];
@@ -47,7 +47,7 @@ export default async function FulhamPage() {
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
       {/* Nav */}
-      <SiteNav active="club-map" club={{ name: 'Fulham FC', color: CLUB_COLOR, tagline: 'The Cottagers · Est. 1879' }} />
+      <SiteNav active="supporter-groups" club={{ name: 'Fulham FC', color: CLUB_COLOR, tagline: 'The Cottagers · Est. 1879' }} />
 
       {/* Hero */}
       <section style={{ padding: '60px 32px 48px', borderBottom: '1px solid rgba(255,255,255,0.06)', background: `linear-gradient(180deg, rgba(204,0,0,0.06) 0%, transparent 100%)` }}>
diff --git a/app/hull/page.tsx b/app/hull/page.tsx
index a890def..3f08372 100644
--- a/app/hull/page.tsx
+++ b/app/hull/page.tsx
@@ -26,7 +26,7 @@ export default async function HullPage() {
 
   const { data: groupsData } = await supabase
     .from('fan_groups')
-    .select('name, city, country, latitude, longitude, url, description, region, type')
+    .select('name, city, country, latitude, longitude, url, description, region, type, instagram_url, facebook_url, tiktok_url')
     .eq('club_id', club.id);
 
   const groups: FanGroupRow[] = groupsData || [];
@@ -47,7 +47,7 @@ export default async function HullPage() {
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
       {/* Nav */}
-      <SiteNav active="club-map" club={{ name: 'Hull City', color: CLUB_COLOR, tagline: 'The Tigers · Est. 1904' }} />
+      <SiteNav active="supporter-groups" club={{ name: 'Hull City', color: CLUB_COLOR, tagline: 'The Tigers · Est. 1904' }} />
 
       {/* Hero */}
       <section style={{ padding: '60px 32px 48px', borderBottom: '1px solid rgba(255,255,255,0.06)', background: `linear-gradient(180deg, rgba(245,161,46,0.06) 0%, transparent 100%)` }}>
diff --git a/app/ipswich/page.tsx b/app/ipswich/page.tsx
index f207265..fa53c4b 100644
--- a/app/ipswich/page.tsx
+++ b/app/ipswich/page.tsx
@@ -26,7 +26,7 @@ export default async function IpswichPage() {
 
   const { data: groupsData } = await supabase
     .from('fan_groups')
-    .select('name, city, country, latitude, longitude, url, description, region, type')
+    .select('name, city, country, latitude, longitude, url, description, region, type, instagram_url, facebook_url, tiktok_url')
     .eq('club_id', club.id);
 
   const groups: FanGroupRow[] = groupsData || [];
@@ -47,7 +47,7 @@ export default async function IpswichPage() {
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
       {/* Nav */}
-      <SiteNav active="club-map" club={{ name: 'Ipswich Town', color: CLUB_COLOR, tagline: 'The Tractor Boys · Est. 1878' }} />
+      <SiteNav active="supporter-groups" club={{ name: 'Ipswich Town', color: CLUB_COLOR, tagline: 'The Tractor Boys · Est. 1878' }} />
 
       {/* Hero */}
       <section style={{ padding: '60px 32px 48px', borderBottom: '1px solid rgba(255,255,255,0.06)', background: `linear-gradient(180deg, rgba(0,68,169,0.06) 0%, transparent 100%)` }}>
diff --git a/app/leeds/page.tsx b/app/leeds/page.tsx
index 46bdbe2..83b36ad 100644
--- a/app/leeds/page.tsx
+++ b/app/leeds/page.tsx
@@ -26,7 +26,7 @@ export default async function LeedsPage() {
 
   const { data: groupsData } = await supabase
     .from('fan_groups')
-    .select('name, city, country, latitude, longitude, url, description, region, type')
+    .select('name, city, country, latitude, longitude, url, description, region, type, instagram_url, facebook_url, tiktok_url')
     .eq('club_id', club.id);
 
   const groups: FanGroupRow[] = groupsData || [];
@@ -47,7 +47,7 @@ export default async function LeedsPage() {
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
       {/* Nav */}
-      <SiteNav active="club-map" club={{ name: 'Leeds United', color: CLUB_COLOR, tagline: 'The Whites · Est. 1919' }} />
+      <SiteNav active="supporter-groups" club={{ name: 'Leeds United', color: CLUB_COLOR, tagline: 'The Whites · Est. 1919' }} />
 
       {/* Hero */}
       <section style={{ padding: '60px 32px 48px', borderBottom: '1px solid rgba(255,255,255,0.06)', background: `linear-gradient(180deg, rgba(29,66,138,0.06) 0%, transparent 100%)` }}>
diff --git a/app/liverpool/page.tsx b/app/liverpool/page.tsx
index d4894b7..54d00e1 100644
--- a/app/liverpool/page.tsx
+++ b/app/liverpool/page.tsx
@@ -26,7 +26,7 @@ export default async function LiverpoolPage() {
 
   const { data: groupsData } = await supabase
     .from('fan_groups')
-    .select('name, city, country, latitude, longitude, url, description, region, type')
+    .select('name, city, country, latitude, longitude, url, description, region, type, instagram_url, facebook_url, tiktok_url')
     .eq('club_id', club.id);
 
   const groups: FanGroupRow[] = groupsData || [];
@@ -46,7 +46,7 @@ export default async function LiverpoolPage() {
   return (
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
-      <SiteNav active="club-map" club={{ name: 'Liverpool', color: CLUB_COLOR, tagline: 'The Reds · Est. 1892' }} />
+      <SiteNav active="supporter-groups" club={{ name: 'Liverpool', color: CLUB_COLOR, tagline: 'The Reds · Est. 1892' }} />
 
       <section style={{ padding: '60px 32px 48px', borderBottom: '1px solid rgba(255,255,255,0.06)', background: 'linear-gradient(180deg, rgba(200,16,46,0.06) 0%, transparent 100%)' }}>
         <div style={{ maxWidth: '800px', margin: '0 auto' }}>
diff --git a/app/manchester-city/page.tsx b/app/manchester-city/page.tsx
index ac6688a..f2cb7e8 100644
--- a/app/manchester-city/page.tsx
+++ b/app/manchester-city/page.tsx
@@ -25,7 +25,7 @@ export default async function ManCityPage() {
 
   const { data: groupsData } = await supabase
     .from('fan_groups')
-    .select('name, city, country, latitude, longitude, url, description, region, type')
+    .select('name, city, country, latitude, longitude, url, description, region, type, instagram_url, facebook_url, tiktok_url')
     .eq('club_id', club.id);
 
   const groups: FanGroupRow[] = groupsData || [];
@@ -46,7 +46,7 @@ export default async function ManCityPage() {
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
       {/* Nav */}
-      <SiteNav active="club-map" club={{ name: 'Manchester City', color: CLUB_COLOR, tagline: 'The Citizens · Est. 1880' }} />
+      <SiteNav active="supporter-groups" club={{ name: 'Manchester City', color: CLUB_COLOR, tagline: 'The Citizens · Est. 1880' }} />
 
       {/* Hero */}
       <section style={{ padding: '60px 32px 48px', borderBottom: '1px solid rgba(255,255,255,0.06)', background: `linear-gradient(180deg, rgba(108,171,221,0.06) 0%, transparent 100%)` }}>
diff --git a/app/manchester-united/page.tsx b/app/manchester-united/page.tsx
index 9f34095..b2ffede 100644
--- a/app/manchester-united/page.tsx
+++ b/app/manchester-united/page.tsx
@@ -26,7 +26,7 @@ export default async function ManUtdPage() {
 
   const { data: groupsData } = await supabase
     .from('fan_groups')
-    .select('name, city, country, latitude, longitude, url, description, region, type')
+    .select('name, city, country, latitude, longitude, url, description, region, type, instagram_url, facebook_url, tiktok_url')
     .eq('club_id', club.id);
 
   const groups: FanGroupRow[] = groupsData || [];
@@ -47,7 +47,7 @@ export default async function ManUtdPage() {
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
       {/* Nav */}
-      <SiteNav active="club-map" club={{ name: 'Manchester United', color: CLUB_COLOR, tagline: 'The Red Devils · Est. 1878' }} />
+      <SiteNav active="supporter-groups" club={{ name: 'Manchester United', color: CLUB_COLOR, tagline: 'The Red Devils · Est. 1878' }} />
 
       {/* Hero */}
       <section style={{ padding: '60px 32px 48px', borderBottom: '1px solid rgba(255,255,255,0.06)', background: `linear-gradient(180deg, rgba(218,41,28,0.06) 0%, transparent 100%)` }}>
diff --git a/app/newcastle/page.tsx b/app/newcastle/page.tsx
index 0ce27a7..f99accd 100644
--- a/app/newcastle/page.tsx
+++ b/app/newcastle/page.tsx
@@ -29,7 +29,7 @@ export default async function NewcastlePage() {
 
   const { data: groupsData } = await supabase
     .from('fan_groups')
-    .select('name, city, country, latitude, longitude, url, description, region, type')
+    .select('name, city, country, latitude, longitude, url, description, region, type, instagram_url, facebook_url, tiktok_url')
     .eq('club_id', club.id);
 
   const groups: FanGroupRow[] = groupsData || [];
@@ -50,7 +50,7 @@ export default async function NewcastlePage() {
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
       {/* Nav */}
-      <SiteNav active="club-map" club={{ name: 'Newcastle United', color: CLUB_COLOR_LIGHT, tagline: 'The Magpies · Est. 1892' }} />
+      <SiteNav active="supporter-groups" club={{ name: 'Newcastle United', color: CLUB_COLOR_LIGHT, tagline: 'The Magpies · Est. 1892' }} />
 
       {/* Hero */}
       <section style={{ padding: '60px 32px 48px', borderBottom: '1px solid rgba(255,255,255,0.06)', background: `linear-gradient(180deg, rgba(36,31,32,0.3) 0%, transparent 100%)` }}>
diff --git a/app/nottingham-forest/page.tsx b/app/nottingham-forest/page.tsx
index 9eb4384..a1c5fdf 100644
--- a/app/nottingham-forest/page.tsx
+++ b/app/nottingham-forest/page.tsx
@@ -26,7 +26,7 @@ export default async function NottinghamForestPage() {
 
   const { data: groupsData } = await supabase
     .from('fan_groups')
-    .select('name, city, country, latitude, longitude, url, description, region, type')
+    .select('name, city, country, latitude, longitude, url, description, region, type, instagram_url, facebook_url, tiktok_url')
     .eq('club_id', club.id);
 
   const groups: FanGroupRow[] = groupsData || [];
@@ -47,7 +47,7 @@ export default async function NottinghamForestPage() {
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
       {/* Nav */}
-      <SiteNav active="club-map" club={{ name: 'Nottingham Forest', color: CLUB_COLOR, tagline: 'The Tricky Trees · Est. 1865' }} />
+      <SiteNav active="supporter-groups" club={{ name: 'Nottingham Forest', color: CLUB_COLOR, tagline: 'The Tricky Trees · Est. 1865' }} />
 
       {/* Hero */}
       <section style={{ padding: '60px 32px 48px', borderBottom: '1px solid rgba(255,255,255,0.06)', background: `linear-gradient(180deg, rgba(221,0,0,0.06) 0%, transparent 100%)` }}>
diff --git a/app/rangers/page.tsx b/app/rangers/page.tsx
index 8291ab7..0d4e826 100644
--- a/app/rangers/page.tsx
+++ b/app/rangers/page.tsx
@@ -26,7 +26,7 @@ export default async function RangersPage() {
 
   const { data: groupsData } = await supabase
     .from('fan_groups')
-    .select('name, city, country, latitude, longitude, url, description, region, type')
+    .select('name, city, country, latitude, longitude, url, description, region, type, instagram_url, facebook_url, tiktok_url')
     .eq('club_id', club.id);
 
   const groups: FanGroupRow[] = groupsData || [];
@@ -47,7 +47,7 @@ export default async function RangersPage() {
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
       {/* Nav */}
-      <SiteNav active="club-map" club={{ name: 'Rangers FC', color: CLUB_COLOR, tagline: 'The Gers · Est. 1872' }} />
+      <SiteNav active="supporter-groups" club={{ name: 'Rangers FC', color: CLUB_COLOR, tagline: 'The Gers · Est. 1872' }} />
 
       {/* Hero */}
       <section style={{ padding: '60px 32px 48px', borderBottom: '1px solid rgba(255,255,255,0.06)', background: `linear-gradient(180deg, rgba(0,102,204,0.06) 0%, transparent 100%)` }}>
diff --git a/app/spurs/page.tsx b/app/spurs/page.tsx
index 58b8a08..146be27 100644
--- a/app/spurs/page.tsx
+++ b/app/spurs/page.tsx
@@ -26,7 +26,7 @@ export default async function SpursPage() {
 
   const { data: groupsData } = await supabase
     .from('fan_groups')
-    .select('name, city, country, latitude, longitude, url, description, region, type')
+    .select('name, city, country, latitude, longitude, url, description, region, type, instagram_url, facebook_url, tiktok_url')
     .eq('club_id', club.id);
 
   const groups: FanGroupRow[] = groupsData || [];
@@ -47,7 +47,7 @@ export default async function SpursPage() {
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
       {/* Nav */}
-      <SiteNav active="club-map" club={{ name: 'Tottenham Hotspur', color: CLUB_COLOR, tagline: 'The Lilywhites · Est. 1882' }} />
+      <SiteNav active="supporter-groups" club={{ name: 'Tottenham Hotspur', color: CLUB_COLOR, tagline: 'The Lilywhites · Est. 1882' }} />
 
       {/* Hero */}
       <section style={{ padding: '60px 32px 48px', borderBottom: '1px solid rgba(255,255,255,0.06)', background: `linear-gradient(180deg, rgba(19,34,87,0.06) 0%, transparent 100%)` }}>
diff --git a/app/sunderland/page.tsx b/app/sunderland/page.tsx
index 0f3a1c5..3fbc91f 100644
--- a/app/sunderland/page.tsx
+++ b/app/sunderland/page.tsx
@@ -26,7 +26,7 @@ export default async function SunderlandPage() {
 
   const { data: groupsData } = await supabase
     .from('fan_groups')
-    .select('name, city, country, latitude, longitude, url, description, region, type')
+    .select('name, city, country, latitude, longitude, url, description, region, type, instagram_url, facebook_url, tiktok_url')
     .eq('club_id', club.id);
 
   const groups: FanGroupRow[] = groupsData || [];
@@ -47,7 +47,7 @@ export default async function SunderlandPage() {
     <main style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', fontFamily: "'Inter', sans-serif" }}>
 
       {/* Nav */}
-      <SiteNav active="club-map" club={{ name: 'Sunderland AFC', color: CLUB_COLOR, tagline: 'The Black Cats · Est. 1879' }} />
+      <SiteNav active="supporter-groups" club={{ name: 'Sunderland AFC', color: CLUB_COLOR, tagline: 'The Black Cats · Est. 1879' }} />
 
       {/* Hero */}
       <section style={{ padding: '60px 32px 48px', borderBottom: '1px solid rgba(255,255,255,0.06)', background: `linear-gradient(180deg, rgba(235,23,43,0.06) 0%, transparent 100%)` }}>
diff --git a/components/FanGroupDirectory.tsx b/components/FanGroupDirectory.tsx
index c6f327e..596f8e1 100644
--- a/components/FanGroupDirectory.tsx
+++ b/components/FanGroupDirectory.tsx
@@ -1,3 +1,7 @@
+'use client'
+
+import { useState } from 'react'
+
 export type FanGroupRow = {
   name: string;
   city: string | null;
@@ -8,6 +12,9 @@ export type FanGroupRow = {
   description: string | null;
   region: string | null;
   type: string | null;
+  instagram_url?: string | null;
+  facebook_url?: string | null;
+  tiktok_url?: string | null;
 };
 
 function typeLabel(type: string | null) {
@@ -16,6 +23,12 @@ function typeLabel(type: string | null) {
   return 'Supporter club';
 }
 
+const linkStyle: React.CSSProperties = {
+  fontSize: '12px',
+  color: 'rgba(255,255,255,0.45)',
+  textDecoration: 'none',
+};
+
 export default function FanGroupDirectory({
   groups,
   clubColor,
@@ -23,6 +36,8 @@ export default function FanGroupDirectory({
   groups: FanGroupRow[];
   clubColor: string;
 }) {
+  const [expanded, setExpanded] = useState<Record<string, boolean>>({});
+
   const regionMap = new Map<string, FanGroupRow[]>();
   for (const g of groups) {
     const region = g.region || 'More worldwide';
@@ -39,54 +54,96 @@ export default function FanGroupDirectory({
 
   return (
     <>
-      {regionOrder.map(region => (
-        <div key={region} style={{ marginBottom: '48px' }}>
-          <h3 style={{
-            fontSize: '13px', fontWeight: 600, letterSpacing: '0.1em', textTransform: 'uppercase',
-            color: clubColor, marginBottom: '16px', paddingBottom: '8px',
-            borderBottom: '1px solid rgba(108,171,221,0.2)',
-          }}>
-            {region}
-          </h3>
-          <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
-            {regionMap.get(region)!.map(group => (
-<a                key={group.name}
-                href={group.url}
-                target="_blank"
-                rel="noopener noreferrer"
-                className="tg-fan-card"
-                style={{
-                  display: 'block', textDecoration: 'none', background: 'rgba(255,255,255,0.03)',
-                  border: '1px solid rgba(255,255,255,0.07)', borderRadius: '10px', padding: '16px 20px',
-                }}
-              >
-                <div style={{ display: 'flex', alignItems: 'flex-start', justifyContent: 'space-between', gap: '12px', flexWrap: 'wrap' }}>
-                  <div style={{ flex: 1, minWidth: '200px' }}>
-                    <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '6px' }}>
-                      <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: clubColor, flexShrink: 0 }} />
-                      <span style={{ fontSize: '15px', fontWeight: 600, color: '#fff' }}>{group.name}</span>
-                      <span style={{ fontSize: '10px', fontWeight: 500, letterSpacing: '0.06em', textTransform: 'uppercase', color: clubColor, opacity: 0.8 }}>
-                        {typeLabel(group.type)}
-                      </span>
-                    </div>
-                    <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.35)' }}>
-                      {group.city ? `${group.city} · ${group.country}` : group.country}
+      {regionOrder.map(region => {
+        const isOpen = !!expanded[region];
+        const count = regionMap.get(region)!.length;
+        return (
+          <div key={region} style={{ marginBottom: '16px' }}>
+            <button
+              onClick={() => setExpanded(prev => ({ ...prev, [region]: !prev[region] }))}
+              style={{
+                display: 'flex', alignItems: 'center', justifyContent: 'space-between', width: '100%',
+                background: 'transparent', border: 'none', cursor: 'pointer', padding: '12px 0',
+                borderBottom: '1px solid rgba(108,171,221,0.2)',
+              }}
+            >
+              <span style={{
+                fontSize: '13px', fontWeight: 600, letterSpacing: '0.1em', textTransform: 'uppercase', color: clubColor,
+              }}>
+                {region} <span style={{ color: 'rgba(255,255,255,0.3)', fontWeight: 400, letterSpacing: 'normal', textTransform: 'none' }}>({count})</span>
+              </span>
+              <span style={{
+                color: 'rgba(255,255,255,0.4)', fontSize: '14px',
+                transform: isOpen ? 'rotate(180deg)' : 'rotate(0deg)',
+                transition: 'transform 0.15s',
+                display: 'inline-block',
+              }}>
+                ▾
+              </span>
+            </button>
+
+            {isOpen && (
+              <div style={{ display: 'flex', flexDirection: 'column', gap: '12px', marginTop: '16px' }}>
+                {regionMap.get(region)!.map(group => {
+                  const links: { label: string; href: string }[] = [];
+                  if (group.url) links.push({ label: 'Website', href: group.url });
+                  if (group.instagram_url) links.push({ label: 'Instagram', href: group.instagram_url });
+                  if (group.facebook_url) links.push({ label: 'Facebook', href: group.facebook_url });
+                  if (group.tiktok_url) links.push({ label: 'TikTok', href: group.tiktok_url });
+
+                  return (
+                    <div
+                      key={group.name}
+                      className="tg-fan-card"
+                      style={{
+                        display: 'block', background: 'rgba(255,255,255,0.03)',
+                        border: '1px solid rgba(255,255,255,0.07)', borderRadius: '10px', padding: '16px 20px',
+                      }}
+                    >
+                      <div style={{ display: 'flex', alignItems: 'flex-start', justifyContent: 'space-between', gap: '12px', flexWrap: 'wrap' }}>
+                        <div style={{ flex: 1, minWidth: '200px' }}>
+                          <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '6px' }}>
+                            <div style={{ width: '8px', height: '8px', borderRadius: '50%', background: clubColor, flexShrink: 0 }} />
+                            <span style={{ fontSize: '15px', fontWeight: 600, color: '#fff' }}>{group.name}</span>
+                            <span style={{ fontSize: '10px', fontWeight: 500, letterSpacing: '0.06em', textTransform: 'uppercase', color: clubColor, opacity: 0.8 }}>
+                              {typeLabel(group.type)}
+                            </span>
+                          </div>
+                          <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.35)', marginBottom: links.length > 0 ? '10px' : 0 }}>
+                            {group.city ? `${group.city} · ${group.country}` : group.country}
+                          </div>
+                          {links.length > 0 && (
+                            <div style={{ display: 'flex', gap: '14px', flexWrap: 'wrap' }}>
+                              {links.map(l => (
+                                <a
+                                  key={l.label}
+                                  href={l.href}
+                                  target="_blank"
+                                  rel="noopener noreferrer"
+                                  className="tg-fan-link"
+                                  style={linkStyle}
+                                >
+                                  {l.label} →
+                                </a>
+                              ))}
+                            </div>
+                          )}
+                        </div>
+                      </div>
                     </div>
-                  </div>
-                  <div style={{ fontSize: '12px', color: 'rgba(255,255,255,0.25)', flexShrink: 0, paddingTop: '2px' }}>
-                    Visit →
-                  </div>
-                </div>
-              </a>
-            ))}
+                  );
+                })}
+              </div>
+            )}
           </div>
-        </div>
-      ))}
+        );
+      })}
 
       <style>{`
         .tg-fan-card { transition: border-color 0.15s, background 0.15s; }
         .tg-fan-card:hover { border-color: ${clubColor}55; background: rgba(255,255,255,0.05); }
+        .tg-fan-link:hover { color: ${clubColor} !important; }
       `}</style>
     </>
   );
-}
\ No newline at end of file
+}
